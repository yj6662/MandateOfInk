# PROGRESS.md — 오행부(五行符) 진행 관리

이 파일은 프로젝트의 **현재 상태 단일 진실(single source of truth)** 이다.
- Claude Code는 매 세션 시작 시 CLAUDE.md 다음으로 이 파일을 읽는다.
- 작업 단위가 끝날 때마다(하루치 체크리스트 항목 완료, 게이트 통과, 결정 확정 등) 이 파일을 갱신한다.
- 체크는 `[ ]` → `[x]`, 부분 완료는 `[~]`(비고에 남은 일 기재).
- 일정 기준: 개발일정 일단위체크리스트 v1.1 (주 5일, D1=착수일). 여기서는 M0~M1만 하루 단위로 펼치고, M2 이후는 게이트 통과 시 v1.1에서 펼쳐 온다.

---

## 현재 상태

| 항목 | 값 |
|---|---|
| 현재 마일스톤 | **M1 체크리스트 전 항목 완료** — 게이트 판정(외부 5인 테스트)만 잔여 |
| 현재 일차 | D28까지 완료. 게이트 통과 시 M2(수직 슬라이스 D29~) 착수 |
| 마지막 갱신 | 2026-07-27 (맵 제작용 KCISA 에셋 19팩 검토 + .gitignore 제외 등록) |
| 다음 할 일 | **황경 시가 블록아웃 착수**(맵 디자인 §9 로드맵 1단계) → 남로 프롤로그 리본 → 동쪽 가도 2.2km. 실플레이 검증(사람): I키 서책 낱장 3D 뷰어, 고서 10장 습득, 원문·번역 감수 → 매핑계획 §6 옵션 판정 → 외부 5인 테스트 |
| 블로커 | 없음 |

---

## S — 선행 스파이크 (권장 · M0 이전 또는 병행)

자모 획 인식은 프로젝트 최고 위험 가정. 본 프로젝트에 붙이기 전 별도 미니 프로젝트로 검증한다.

- [x] S1. 빈 Unity 프로젝트(또는 콘솔)에서 $1 Unistroke Recognizer C# 포트 구동 — 콘솔로 검증(리포 루트 `Spike/S1_PDollarConsole`, PDollar 원본 소스 링크 + Mathf 셔밍). 동봉 16종 템플릿: 자기 분류 16/16, 교란 변형(지터 2%·크기 0.7~1.3배·회전 ±10도·점 15% 솎기) 1,580/1,600 = **98.8%**, 분류 지연 평균 **0.59ms**/p95 0.75ms. 유일한 약세는 asterisk 82/100(육각별과 혼동) — 형태가 비슷한 클래스 간 혼동 가능성 확인
- [x] S2. 초성 5자(ㄱㄴㅁㅅㅇ) 템플릿 등록 → 마우스 입력 인식률 측정 — `Assets/Spike/S2_JamoTest.unity` 플레이모드에서 실측. 템플릿 23개 등록(ㄱ5·ㄴ5·ㅁ5·ㅅ5·ㅇ3), 사용자 실기 테스트에서 정확 인식 확인(정량 수치는 별도 기록 안 함)
- [x] S3. 다획 자모(ㅁ 등) 처리 전략 검증 — 획 분할 vs $N/$P 계열 필요 판단 → **이미 $P(PDollar) 사용 중이라 다획 네이티브**. 실증: ㅁ 3획/1획(한붓쓰기), ㅅ 1획/2획 혼재 템플릿이 전부 정상 인식. 획 분할 로직 불필요
- [x] S4. 판정: 인식률·지연이 실사용 가능 수준인가 → **통과, 결정 로그 기록**. S1 합성 교란 98.8%·평균 0.59ms + S2 실기 확인. M1 게이트(외부 5인)에서 재판정 예정

## M0 — 셋업 · 프리프로덕션 (D1–D8)

- [x] D1. Unity 6·URP 프로젝트 생성, Git+LFS·.gitignore — Unity 6000.3.9f1 · URP 17.3.0. .gitignore는 Unity 프로젝트 폴더에 배치, .gitattributes(LFS)는 리포 루트에 작성
- [x] D1. 초기 세팅 — Force Text 직렬화 · Visible Meta Files · Linear 색 공간 · URP 프리셋 (전 항목 확인 완료)
- [x] D1. 폴더 구조 + asmdef 모듈 경계 확정(Core / Spellcraft / Combat / Data / Presentation / EditorTools — 런타임 코어 Odin 참조 금지) → 「프로젝트 메모」에 기록 완료
- [x] D2. 코어 패키지 도입 — VContainer **1.19.0** · UniTask **2.5.11** 설치(OpenUPM), Input System 1.18.0 기설치, Odin Inspector 임포트 완료(`Assets/Plugins/Sirenix`, 에디터/데이터 계층 한정 · 런타임 코어 참조 금지)
- [x] D2. 부트 씬 + 루트 LifetimeScope, 앱 시작 흐름(부트 → 인게임 스텁) 골격 — `Core/App/RootLifetimeScope.cs`(+DontDestroyOnLoad)·`AppEntryPoint.cs`(IAsyncStartable, UniTask 씬 전환), Boot·InGame 씬, 빌드 세팅 등록(Boot=0). 플레이모드에서 부트→인게임 전환 로그 확인 완료(2026-07-19)
- [x] D3. ScriptableObject 이벤트 채널 코어(제네릭 채널·리스너 + 에디터 디버그 표시) — `Core/Events/`: EventChannelSO<T>·VoidEventChannelSO(+ContextMenu 디버그 발신·발신 카운터)·리스너 2종(UnityEvent 반응)
- [x] D3. 핵심 서비스 인터페이스 정의·DI 등록 — `Core/Services/`: ISceneLoadService(실구현)·ISaveService·ISoundService·IPoolService(스텁), RootLifetimeScope에 싱글턴 등록
- [x] D3. 부트 흐름 검증(싱글턴 없이 DI·이벤트 채널로만 연결) — AppEntryPoint가 ISceneLoadService를 생성자 주입으로 받아 씬 전환. 플레이모드 [SceneLoad] 4단 로그 확인 완료(2026-07-19)
- [x] D4. 1인칭 컨트롤러(WASD·마우스 시점) + 기본 카메라 — **StarterAssets FirstPerson 활용**(사용자 제안·합의). InGame 씬에 NestedParent_Unpack 배치(언팩 완료)·프로토 바닥(Plane 50m)·기본 카메라 제거·모바일 조이스틱 UI 삭제. 플레이모드 WASD·마우스룩 검증 완료(2026-07-19)
- [x] D5. 작도 키 홀드 상태머신(교전↔작도) + 뷰모델 별도 카메라·레이어 — 상태머신·시간 감속 구현: `Spellcraft/SpellcraftModeController`(Combat↔Drawing, timeScale+fixedDeltaTime 감속·복원), `Data/SpellcraftModeConfigSO`([가정] Q 토글·0.2배), `EC_DrawingModeChanged`(bool 채널) → ProtoGlue가 StarterAssets 입력·커서 잠금/해제 중계. InGame 배선 완료. 모드 전환·감속·커서 플레이 검증 완료. 뷰모델: ViewModel 레이어(6) 신설, MainCamera 컬링 제외, ViewModelCamera(URP Overlay, FOV 50 [가정], near 0.01) 스택 등록, 대필 자리표시자 큐브([가정] W2에서 교체). 자리표시자 표시·추적 플레이 확인 완료(2026-07-19)
- [x] D6. 데이터 스키마 — 도면 DTO·상극 5×5(SerializedScriptableObject + Dictionary)·적 DTO·강화 DTO — `Data/`: Element·Polarity·FinalModifier enum, EffectParam 추상 다형성(Damage/Shield/Dot), SpellDiagramSO(글자·자모·파생속성·먹비용·효과), ElementRelationTableSO(중첩 Dictionary + GetMultiplier), EnemyDefinitionSO, UpgradeDefinitionSO(마석 비용 — 화폐 아님 명시). `ElementRelationTable.asset` 생성·5×5 기본값([가정] 우세 1.5/열세 0.5/중립 1.0) 채움·조회 검증(수→화 1.5, 금→화 0.5, 목→목 1.0). Odin 직렬화 파일 저장 확인
- [x] D7. 글자 일람 v1.2 → 도면 DTO 임포트(40장), 로드·조회 검증 — **120장 전체 임포트**(요구 40장의 상위집합). PDF→CSV 추출(`Assets/Docs/오행부_작도글자일람_v1_2.csv`, 120행 원문 대조 100% 일치), EditorTools `SpellDiagramCsvImporter`(메뉴·멱등, Effects 수동 튜닝 보존) → `Data/Diagrams/SD_*.asset` 120개 생성, 조회 검증(나=화·양·단일·공격). 주의: 일람 v1.2 축 기준으로 스키마 수정 — 광역은 중성(천지인 Scope), 종성=속박/지속/격발/관통/연쇄
- [x] D8. 플레이스홀더 — 졸개 적(캡슐+텔레)·대필 막대·회색 아레나 — 더미 적(EN_TestDummy 금속성) + EnemyAI 명시적 상태머신(대기→추적→텔레그래프[몸 붉어짐, 시간 감속 영향]→공격→회복, 수치는 SO), PlayerHealth+PlayerConfigSO(HP HUD — 허용 항목), 회색 아레나 벽 4면(M_Proto_Wall), 대필 막대는 D5 자리표시자. GAP VFX·시전→격파 파이프라인 포함

**게이트 M0**: [x] **통과(2026-07-19)** — 아레나에서 1인칭 이동 + 원거리 텔레그래프 적 교전 + 작도(이어 그리기 인식)/교전 모드 전환 전부 작동. 싱글턴 없음 — VContainer DI + SO 이벤트 채널로만 연결. 사용자 플레이 확인.

## M1 — 프로토타입: 작도 손맛 + 룩 (D9–D28, 최우선)

### W1 — 작도 인식 코어
- [~] D9. 작도 평면 + 마우스 투영, 날것 점 궤적 수집·시각화 — M0에서 선행: DrawingInputController가 화면 좌표 궤적 수집 + ViewModel 레이어 먹선 표시(카메라 추종). 작도 평면·투영 정식화는 M1에서
- [~] D10. $1류 제스처 인식기 통합(스파이크 결과 반영) + 초성 5 단독 인식 — M0에서 선행: PDollar에 asmdef 부여(Spellcraft 참조), 템플릿 23개를 _Project/Data/JamoTemplates로 이관·런타임 로드, 초성 인식→ㅏ 기본형 도면 매핑([가정] 임시 규칙)→EC_DiagramDrawn 채널→시전→모드 자동 해제. 인식 품질·정식 통합은 M1에서 재검
- [~] D11. 중성 4(ㅏㅓㅗㅜ) 인식 + 초·중 결합 20장 — M0에서 선행: 획 그룹 분할 인식(초성 우선 쓰기 규칙 이용, 부분별 독립 정규화 — 위치·침범 무관) + F2 중성 실필기 등록. 사용자 체감 「굉장히 좋아짐」(2026-07-19). 정량 측정·정식화는 M1에서
- [x] D12. 받침 인식 + 도면 DTO 조회 → 진 식별 연결 — 3분할 탐색+공간 게이트, 종성 5종(초성 템플릿 재사용), 폴백 체계. 검증 완료(2026-07-20)
- [x] D13. 획 품질 3축 스코어 + 폴백(약발동) + 먹선/날것 점 분리 — 3축 [제안: 형태(최악 자모 거리)/구조(평균 거리)/속도(실시간 작도 시간 vs 획수 기대치)] 가중 평균 -> 위력 0.85~1.1 배율. **인식 입력을 스무딩 붓 위치 -> 날것 마우스 점으로 교정**(§11 위반 수정). 검증 완료 — 같은 글자 속도차가 피해차로 발현(2026-07-20)

### W2 — 작도 뷰모델(대필·석경)
- [x] D14. 대필 자루끝 축 겨누기 회전 + 정적 손 — **자루끝 축 겨누기 구현 완료**: 손잡이 끝 오른쪽 아래 고정 축, 작도 중 붓끝이 커서를 겨누며 추종(길이 신축 0.55~1.8배 [가정]). Meshy FBX cm 단위 문제 해결(useFileScale 끔 — Meshy 모델 공통 처리 필요), 붓 0.8m. 정적 손은 M1 폴리시
- [x] D15. 스프링 무게감 + 먹선 — 붓끝 반음감쇠 스프링(관성·출렁임, 표현 전용·인식 분리), 먹선은 InkStroke 메시(TrailRenderer 상위 호환)로 충족. 검증 완료(2026-07-20)
- [x] D16. 모드 전환 자세 디제틱 — 방향 토글(평타 후 자루끝 전방 유지 <-> 작도 진입 시 붓끝 복귀), 평타 = 자루 휘두르기 스윙(100도 호·파고듦·전방 밀림, 보간 분리 가산), 적중 화면 울림. 뒤집힘 자세는 플레이 모드 스크린샷 반복으로 확정. 검증 완료(2026-07-20)
- [x] D17. 근평면·벽 클리핑 + 깊이·크기 1차 — 뷰모델 레이어·오버레이 카메라 분리 확인, 대필 1.5배(실측 1.2m)·손잡이 끝 원점 재정렬 캘리브레이션(붓끝=커서 일치). **W2 종료**. 잔여 튜닝: 평타 리치 연장(1인칭 체감, 사용자 메모) 등 손맛은 게이트 실측에서

### W3 — 교전 동사 + 먹 경제
- [x] D18. 회피(무적 프레임) + 클릭 평타 — DodgeController(Shift 탭=이동 방향 무적 대시·홀드=질주 유지, 사용자 결정), MeleeAttack(좌클릭 자루끝 스피어캐스트 — 딜링 아닌 먹 버는 동작, 피해 3·환급 6 [가정]). 기능 검증 완료(2026-07-19)
- [x] D19. 단일 먹 풀 — 트리클 + 교전 충전 + 비상 수동 갈기 — InkPool(100 [가정]·트리클 1.5/s·시작 가득), 시전 시 InkCost 소모+**쥐어짜기**(부족 시 잔량 비율 위력·전량 소모 — 사용자 결정), R 홀드 갈기(예열 0.35s·30/s·피격 취소 — 사용자 결정), 평타 환급. 임시 잉크 미터 HUD. 기능 검증 완료 — 트리클 1.5→0.3/s 하향(체감상 소비를 따라잡던 문제, 사용자 피드백)
- [x] D20. 디제틱 잔량 + 부적(즉발·제한 수량) — TalismanController(교전 숫자키 1~3, 즉발·먹 무소모·수량 차감, 시작 슬롯 [가정] 나x3·우x2·가x2, 제작/충전 UX는 미결 유지), BrushInkTint(잔량 30% 이하부터 붓이 바램 — 벼루 아이콘 전 1차 디제틱). 부적 수 HUD(허용 항목). 검증 완료(2026-07-20)
- [x] D21. 졸개 텔레그래프(느린/빠른/페인트, 0.8~1.2초) — EnemyAI 3종 어휘 이식: 느린(진홍 1.2s 강타탄 x1.6)/빠른(주황 0.8s 속사탄 x0.7)/페인트(진홍 **위장** 0.7s+멈칫 0.5s 지연탄 — 성급한 회피 처벌, 멈칫 중 조준 정지). 시간·배율은 EnemyDefinitionSO로 데이터화. 검증 완료(2026-07-20)
- [x] D22. 적 피해·체력·기본 AI 상태머신 + 한 합 검증 — 검증장(보스 사정권 밖 -28,-18/-22,-26) 졸개 2기, 접근→예비 동작 읽기→회피(무적 프레임)→작도 반격 한 합 검증 완료(2026-07-20). **W3 종료**

### W4 — 받아치기 · 그로기 · 상합 + 룩
- [x] D23. 받아치기(선제 상극) + 속성 3단계 판정 — 상극 방어 진으로 판정(사용자 설계): 우세=쳐내기(포이즈 45%)/중립=차단/열세=막 파괴. 전방 막 1.2s로 단축(패링 타이밍화). 검증 완료(2026-07-20)
- [ ] D24. 막 받아치기 보너스 + 먹 환급 — 패링 성공 시 먹 환급 잔여
- [x] D25. 그로기 누적·감소 + 급소 창 폭딜 — 포이즈 축적(술식 50%·평타 8·패링 45%)→3s 그로기(피해 x1.5)·자연 회복 6/s. 검증 완료(2026-07-20)
- [ ] D26. 그로기 표시 — 본체 「틈」 표시 완료, 락온 레티클 연동 잔여
- [x] D27. 상합(격발) — ㅁ받침 표식 설치→다음 술식 적중 시 격발 x2.2. 격발 조건 [가정] 단순 규칙(음/양·5x5 매트릭스 확정 시 데이터 교체). 검증 완료(2026-07-20)
- [x] D28. 수묵 룩 프로토 + 게이트 빌드 + 테스트 준비 — 임시 코드 셰이더 스탠드인(ToonLitTemp+InkPostTemp, 에셋 구매 시 교체), **게이트 빌드 성공**(GateBuild.cs 메뉴/배치, Builds/Gate_M1 131MB·경고 2), 외부 5인용 테스트 가이드 v0.1(조작·글자 일람·§13 검증 10문항) 작성. **외부 테스트 실시·게이트 판정은 사람 일정**(2026-07-20)

**게이트 M1**: [ ] 외부 5인 손맛·룩 긍정 / 자모 인식률·좌절 임계 통과 / 0.8~1.2초 내 2획 상극을 0.5초 창에 / 먹 자기조절·모드 가독성 체감.

## M2 이후 (요약 — 게이트 통과 시 v1.1에서 하루 단위로 펼쳐 옴)

- [ ] M2. 수직 슬라이스 — 황경 처음부터 끝까지 (D29–D53)
- [ ] M3–M4. 압축 프로덕션 — 2강토·종성 전체·상극 5×5·메카천수관음 (D54–D93)
- [ ] M5. 알파·데모 — 스팀 페이지·넥스트 페스트·위시리스트 2만 목표 (D94–D113)
- [ ] M6. 베타·폴리시·출시 (D114–D133)

---

## 결정 로그

확정된 결정만 기록한다. [제안] 단계는 「미결 사항」에 둔다.

| 날짜 | 결정 | 근거/비고 |
|---|---|---|
| 2026-07-19 | 먹 잔량 UI = **찰랑거리는 먹물이 담긴 벼루 아이콘**으로 표현 예정 | 사용자 결정(방향). 현 임시 텍스트 미터의 대체재 — 구현 시기는 UI/룩 단계. 먹 체감: 그리는 중 실시간 소모, 잔량 10% 미만 급격 옅어짐(비선형), 연한 먹 발현(회백·흐림·축소) |
| 2026-07-19 | W3 입력·규칙: 회피=**Left Shift 탭**(홀드=질주 겸용), 먹 부족=**쥐어짜기**(잔량 비율 위력·전량 소모·불발 없음), 수동 갈기=**R 홀드 즉시 도입** | 사용자 결정. 수치는 CombatConfigSO [가정] |
| 2026-07-19 | 약발동(A안) 채택 — 임계 미달 시 최근접 글자를 위력 50%로 발동, 완전 불발 금지 유지. 정발동=먹선 글로우 소멸/약발동=무효과 서서히 소멸. 작도 입력 = Q 토글 -> **홀드**(누른 채 그리고 떼면 판정) | 사용자 결정. 임계·배율은 SpellcraftModeConfigSO([가정] 거리 1.8·배율 0.5) |
| 2026-07 | 아키텍처: VContainer DI + SO 이벤트 채널 + 상태머신 + asmdef, "The Last One" 폐기 | 체크리스트 v1.1 반영 |
| 2026-07 | Odin: 에디터/데이터 계층 한정, 런타임 코어 독립. SerializedScriptableObject·Dictionary 직렬화·OdinMenuEditorWindow 패턴 | Personal 라이선스, Validator 스킵 |
| 2026-07 | 분기: 순수 플래그(필수/금지) + 카운터 2종. 원장 CSV = 단일 진실 | 분기 시스템 사양 v0.3 |
| 2026-07 | 문서 체계: 사람용 PDF 단독 / 개발용 MD·CSV | zip 방식 대체 |
| 2026-07-19 | 생성형 AI 스택 확정: 3D=**Meshy** / 2D·UI=**Recraft** / 사운드·음성=**ElevenLabs + SuperTone** (Hyper3D·Suno 미채택) | 사용자 결정. 키는 Tools/.env(gitignore), 4종 인증 검증 완료 — ElevenLabs는 TTS 전용 제한 키(관리용 읽기 권한 없음, 사용엔 지장 없음) |
| 2026-07-19 | 1인칭 컨트롤러 = **StarterAssets FirstPerson 채택(프로토타입 한정)** | 사용자 제안. asmdef 없이 Assembly-CSharp 소속 — 우리 모듈에서 직접 참조 금지, 플레이스홀더 취급. D5 작도↔교전 전환 통합 시 재평가 |
| 2026-07-19 | 작도 모드 = **키 진입 + 시간 감속, 작도 완료 시 해제·시간 복원** | 사용자 결정. D5 골격은 같은 키 토글로 해제, M1에서 자모 인식 성공 이벤트가 해제를 대신. 감속 배율 등 수치는 SpellcraftModeConfigSO([가정] 0.2배) |
| 2026-07-18 | 작도 인식기 = PDollar **$P Point-Cloud Recognizer** 채택. 다획 자모는 획 분할 없이 $P 네이티브 처리 | S 스파이크 통과 — S1 합성 교란 98.8%·0.59ms, S2 실기 마우스 정확 인식, S3 ㅁ(1·3획)/ㅅ(1·2획) 혼재 인식. M1 게이트에서 외부 테스트로 재검증 |

## 미결 사항 (임의 결정 금지)

- [ ] 산군 배치: 인왕산 1차 vs 청림 완전판
- [ ] 착호 병과 채택 여부
- [ ] 마물 이중 명칭(관보식/민간) 채택 여부
- [ ] 황경·조정 톤 방향(신(信) 변질, 인정·파루·순라 앵커) — 제안 단계
- [ ] 무학대사 정체
- [ ] 황경 보스 「개천 이무기」 채택 여부 — [제안]

## 문서 정비 잔무

- [x] CLAUDE.md 종성·중성 절 v2.0 개정(2026-07-19) — 광역=천지인, 종성=속박/지속/격발/관통/연쇄, 예시 교체
- [ ] 게임기획서 v1.4 §8 구 아키텍처 언어 정리(차기 개정 시)
- [ ] v1.0 개발 체크리스트 PDF를 프로젝트 지식베이스에서 제거

## 리스크 감시

| 리스크 | 상태 | 대응 |
|---|---|---|
| 자모 획 인식 실패(최고 위험) | S 스파이크 통과(2026-07-18) — 1인 검증 | M1 게이트에서 외부 5인·중성/받침 확장으로 재판정 |
| Suno 산출물 저작권 불확실성 | 상존 | 출시 전 사람 검토, 유료 상업 이용권 유지 |
| 셰이더 에셋 URP Render Graph 비호환 | 미확인 | 구매 전 에셋별 호환 확인 |
| ~~프로젝트 URP 파이프라인 에셋 부재~~ | 해결(2026-07-19) | `_Project/Settings/URP_PipelineAsset` 지정 완료 — 삭제 금지 |

## 프로젝트 메모 (작업하며 갱신)

- 리포 구조: Git 루트 = `C:\Users\yj666\MandateOfInk`, Unity 프로젝트 = 하위 `MandateOfInk/`. `.gitattributes`(LFS)는 루트, `.gitignore`는 Unity 프로젝트 폴더.
- 폴더 구조: 프로젝트 고유 자산은 전부 `Assets/_Project/` 아래(서드파티와 분리). 하위: `Art / Audio / Data(SO 에셋 인스턴스) / Prefabs / Scenes / Settings / UI / VFX / Scripts`. `Scripts` 하위 = asmdef 모듈 6종: `Core / Data / Spellcraft / Combat / Presentation / Editor(EditorTools)`. 서드파티(PDollar·GabrielAguiarProductions·Plugins 등)는 `Assets/` 루트 유지.
- asmdef 의존 방향(런타임): Core ← Data ← Spellcraft ← Combat ← Presentation (상위가 하위 참조, 역참조 금지). EditorTools는 Editor 전용 플랫폼으로 전 모듈 참조 가능 — Odin 참조는 EditorTools(및 추후 데이터 SO 계층)에만 허용, 런타임 코어 금지.
- 네이밍 규칙: 어셈블리·네임스페이스 `MandateOfInk.<모듈>`(예: MandateOfInk.Spellcraft). 클래스·메서드·프로퍼티 PascalCase, private 필드 `_camelCase`, 로컬·매개변수 camelCase. SO 에셋 파일명 PascalCase, 이벤트 채널 SO는 `EC_` 접두(예: EC_PlayerDamaged) — [가정] 필요 시 조정.
- 루트 `CLAUDE.md`(Unity 프로젝트 폴더)는 `Assets/Docs/CLAUDE.md`를 @임포트하는 연결 파일. 지침 원본은 Assets/Docs에 유지.
- 빌드·테스트 명령: (확정 후 CLAUDE.md에도 반영)

## 세션 로그

최신이 위. 형식: 날짜 / 한 일 / 다음 할 일 / 특이사항.

| 날짜 | 한 일 | 다음 할 일 | 특이사항 |
|---|---|---|---|
| 2026-07-27 | **맵 제작용 에셋 19팩 전수 검토 + git 제외(사용자 요청).** ① .gitignore에 서드파티 팩 전부 등록(폴더+루트 .meta+Scenes에 풀린 데모 씬 House_1/2.unity+BuildReport) — 미추적 에셋 0 확인. **주의: 씬은 커밋되지만 에셋은 리포 밖 — 새 머신 클론 시 에셋스토어 재다운로드 필요**. ② 셰이더 호환 표본 감사(12개/팩): **URP 네이티브** = 화성성문·화성행궁·세연정·Korea_TreasureProps·KTinteractiveProp·House_1/2 / **셰이더그래프(URP 호환)** = KTP_Decal·KTP_Normal_Vol.2·KT_SmartMat_Vol_6·단청문양이펙트 / **빌트인 Standard(분홍 위험 — 사용 전 변환 필요)** = **제주목관아·SmartMaterials Vol.3/4·Korean_Traditional_Prop** / 자체 커스텀 셰이더 = 낙안읍성(PBR_Shaders 3종)·용머리해안(39종) — 씬 배치 후 렌더 확인 필요. ③ 용도 매핑: 성문=도성 4문, 행궁=경복궁·관아군, 낙안읍성=서민 시가·어귀 마을, 세연정=성소, 스플라인빌더=가도·성벽, 용머리해안=한강·나루 지형, 단청이펙트=진 이펙트 후보, KTP 데칼/노멀·SmartMat=표면 라이브러리, Hierarchy Designer·BuildReport=에디터 툴 | 황경 블록아웃에서 성문·행궁·읍성 우선 투입, 사용 직전 해당 팩만 ToonLit 스왑 또는 텍스처 승계(M_Field_Stone 선례) | **미결(사용자 확인)**: 기추적 GabrielAguiarProductions·House Of Changwon·MobileDependencyResolver도 git에서 제외할지(git rm --cached 필요 — 리포 이력에서 앞으로 빠짐). KTinteractiveProp CS 5개는 asmdef 없음(Assembly-CSharp행 — 직접 참조 금지 규율 유지) |
| 2026-07-27 | **여정·맵 문서 개정 — 사용자 확정 3건 반영.** ① **자음군 확장 폐지**: 전 자모 시작부터 사용 가능, 성장은 마석 등급·도면·강화로 일원화("쓸 줄 아는 것"과 "제대로 터지는 것"의 간극이 성장). 연쇄 수정 — 청림 역전 체감의 축을 자모 해금→**금 마석 등급**으로 이전(청림에서 금 술식은 그려지나 저등급이라 약함 → 철옹 등급 해금 후 같은 획이 강해짐). **주의: 기획서 v1.4 §6 강토=자음군 해금 표·프로젝트정리.md 해당 줄은 구버전** — 이 결정이 우선. ② **귀로 설계 폐지 → 배달 체인**: 완료 지점에서 다음 수주(전 거점=수주처), 체인이 순환 노선을 그려 전진만으로 세계를 꿰고, 지름길은 사망 회수·탐험 편의용으로 한정(맵 §7.5 재작성). ③ **진행 연동 월드 침식**(여정 §7 신설, 엘든링 로데일→잿빛 왕도 문법): 전역 단계 W0평시→W1조짐(청림 보스)→W2침식(황경 심부 사건)→W3절정(현강 진입, **현강만 무풍 — 마석 없는 땅의 역설**), 트리거는 순수 플래그(분기 원칙과 동일). 침식이 배달 체인의 같은 길 재사용을 정당화(풍경·적·의뢰 내용 변화). 청림 보스 격파의 "정화"도 국소 진정으로 수정(뿌리는 남음 — 침식 복선). 구현 레버 [제안]: 별도 씬 아닌 데코 레이어 토글+머티리얼 스왑, SaturationTriggerZone·_MaterializeErode 재활용, ISaveService 연동 전제 | 황경 시가 블록아웃 착수 | 침식 단계 상세·트리거는 스토리 설계 메모 v0.5 사건 순서와 대조 필요(미결). 침식으로 노선 막힐 땐 대체 노선 선개방 원칙 명기 |
| 2026-07-27 | **플레이어 여정 v0.1 작성(`오행부_플레이어여정_v0_1.md`) — 맵 디자인의 시간 축 전개(사용자 요청: 이동 경로·도달 시점의 플레이어 수준·월드 상태를 md로).** ① 레벨 없는 게임의 「수준」 3축 정의: 숙련(손 — 받아치기 성공률·작도 속도)/성장(장비 — 마석 등급·도면 수)/권한(차패 등급 — 통과 범위) ② 1차 출시 페이즈 0~5(0~12h): 프롤로그 남로 30분 → 황경 정착(~2h, 동문만 개방) → 동쪽 가도·청림 어귀(~4h, 첫 필드 작도) → **청림 심부·첫 대보스(~6.5h — 1차 숙련 관문**, 받아치기 50%+ 기대, 오염 숲 정화 전후 대비 연출) → 황경 재방문(승급 차패 개방·재방문 의미 변화) → 클라이맥스(시가 던전화, 2번째 보스는 미결 유지) ③ 완전판 6~10(12~30h): 적로→철옹(치음 해금=청림 역전 체감 완성)→무악재 최대 개방(~20h 카타르시스)→현강·개성(**차패 무효지 — 이동 문법 반전**, 분기 플래그 집중)→엔딩 4종 ④ 게이팅 총괄표(잠김의 절반=권한/절반=실력·술식, 키 아이템 잠금 금지 원칙) ⑤ 페이스 리듬(대개방 1~1.5h당 1회, 이동 수단 비율 변화, 사망 압력 단계) | 황경 시가 블록아웃 착수(변동 없음) | 미결 추가 식별: 자모 해금 해석(시작 5자 vs 강토 자음군 표의 정합 — 작도 시스템 문서 개정 필요), 시가 차량 통행 금지 여부, 1차 돈의문 개방 여부 |
| 2026-07-27 | **맵 디자인 v0.1 골격 확정 + 보강(사용자 결정: 배달 사이클 5~10분 · 1차 인접 강토=청림(東) · 시작=남로 · 여지도 UI는 실 topology에 대동여지도 스킨을 입혀 재제작, 현행 생성 지도는 임시).** 문서 추가 4개 절: **§6.5 청림 내부 개요**(평야→숲 어귀→덮인 숲→심장부에서 오염으로 먹빛 회귀하는 반전, 어귀 마을·성소·오염 신목 던전 [가정], 금 상극 미해금 상태의 역전 체감 설계) / **§7.5 귀로·지름길**(양방향 비대칭 풍경 — 귀로 정면의 도성 실루엣, 소울라이크 루프백 지름길이 여지도에 실선으로 남는 개척 보상, 귀로 전용 사건) / **§8.5 필드 작도 지형 편입**(앵커=레벨 디자이너의 붓 — 뭄 돌다리=지름길 해금 표준, 굼=수직 지름길, 언=외사산 낙사 험지와 짝, 표식은 근거리 확인 장치·원거리 인력은 지형 담당) / **§9.5 여지도 제작 사양**(블록아웃 부감→대동여지도 문법 재작화→개척 시 판본 갱신 연출 [제안]) + §7에 배달 사이클 검산표(표준 편도 6~7분=중앙값 적중, 청림 심부는 중간 인계 분할로 상한 준수 — 가도 2.2km 유지 확정) | 황경 시가 블록아웃 착수(§9 로드맵 1: 성벽+4문+육조거리 축+개천+낙산 언덕, 기존 마을 배선 이식) | 남은 미결: 인왕산 산군·청림 던전/보스 실체·Terrain 도입·인정/파루 톤 — §10에 확정/미결 분리 기록 |
| 2026-07-27 | **맵 디자인 v0.1 문서 작성(`오행부_맵디자인_v0_1.md`) — 한양 실지형 기반 오픈월드 유도 설계(사용자 요청: 지점별 유도·첫 풍경·경험을 최대한 상세히, 실지형 왜곡 허용).** 근거: CEDEC 2017 BotW 발표(인력·필드 삼각형 3단·세 척도 — 사용자 제공 4Gamer 원문)+엘든링 유도 분석(차폐→굽은 길→개방)+실제 서울 내사산/외사산 이중 분지·조선 간선로. 골자: ① 오행부식 인력 사전 — 수묵 단색 세계에서 **색을 띤 것이 곧 인력**(주막 등불·오행 담채·마석 발광), 굽은 길 절대 규칙 ② 세 척도 — 압축률 상수(시가 1/10=도성 400m, 가도 1/25), 밀도 상수(차 기준 20~30초/시각 이벤트 1), 척도감(포켓 20~40분) ③ 방위 배치 — 東 청림(최저 저항: 낙산→중랑천 평야)·南 적로(한강 도하 관문)·西 철옹(마포 하류)·北西 현강(무악재→임진강→개성, 후반)·正北 북한산=길 아닌 도전 지대, 내사산/외사산 이중 링 난이도 ④ 시작 시퀀스 [제안]: 남로 한강 남안 → 5분 한강 대개방 → 나루 도하 → 숭례문 차패 결계 → 육조거리 시선축(경복궁+백악 정면 구도) ⑤ **「처음 보는 풍경」 지점표 9개**(E1 흥인지문 밖 초록 담채 평야 / S1 한강 대여백+적로 연기 / N2 무악재 마루 임진강+송악산 최대 개방 등 — 강토 진입마다 그 오행 색이 "처음" 화면에 드는 공통 문법) ⑥ 가도 문법(직선 최대 250m·동쪽 가도 2.2km 배치안)·도보 포켓 문법(좁아졌다 열림·끝 조망) ⑦ M2 정렬 로드맵(황경 블록아웃→남로 프롤로그→동쪽 가도→포켓 2) | **사람 판정(§10)**: 인접 강토=청림 확정, 시작=남로 채택, 배달 사이클 5~10분 확정(가도 길이 근거), 인왕산 산군은 미결 유지 → 판정 후 황경 시가 블록아웃 착수 | 네이버 블로그 글=4Gamer 원문 동일 확인(사용자). 문서는 전부 [가정]/[제안] 표기 — 블록아웃 실측으로 확정. PDF 인쇄본 필요 시 WeasyPrint 변환 |
| 2026-07-27 | **최적화 패스 — 스크립트 메모리 누수·GC·그래픽(사용자 요청, 룩 무변경 원칙).** ① **인스턴스 머티리얼 누수 근절(최대 항목)**: `renderer.material` 인스턴스는 GameObject 파괴 시 자동 해제되지 않아 시전마다 누적됐음 — SpellVisuals에 **RuntimeMaterialCleaner**(정적 Track, GO 파괴 시 등록 자원 일괄 Destroy, 중복 등록·중복 Destroy 안전) 신설, 누수 지점 전부 등록: CreateTranslucent(투사체·파동·방패·소환수·동전·표식 등 전 프리미티브)/AttachLetter(글자당 새 머티리얼)/ApplyTint·MultiplyAlpha(**패턴 프리팹 렌더러별 인스턴스 — 매 시전·폭발마다 다수 생성되던 최다 빈도 누수**)/FieldSpellExecutor 폴백 비주얼. 기존에 스스로 정리하던 GroundBlast·SpellShield·SpellAreaBlast·SpellBurstVisual은 그대로(Destroy 멱등). ② **프레임당 GC 제거**: InkStrokeFader가 페이드 중 매 프레임 Color32 배열 2개를 새로 할당 → 스크래치 버퍼 재사용으로 제로 할당. ③ **자원 수명 보강**: DrawingInputController(먹선 머티리얼+절차 아틀라스 텍스처 OnDestroy 해제), CodexPageViewer(한지판·광곽 머티리얼+RenderTexture Destroy — Release만 하던 것 보강). ④ **OnGUI 경량화**: 7개 프롬프트 컴포넌트 전부 useGUILayout=false(레이아웃 패스 생략), HaeryePagePickup 프롬프트 문자열 캐시(+출전 표기로 개선 — "해례본" 고정 문구가 새 고서 4종과 안 맞던 것 수정). ⑤ **그래픽 감사**: 텍스처 전수 조사 — 전부 ≤2048로 이미 적정(다운사이즈 불요), 셰이더 실체화/침식 분기는 기본값에서 이미 스킵됨(수정 불요), **UI 전용 텍스처 6장 밉맵 해제**(오버레이 UI엔 무용 — 메모리 ~33% 절약). 컴파일 검증(scriptCompilationFailed=False·신규 심볼 로드 확인) | 실플레이(사람): 전투 몇 합 후 이상 없는지(연출 동일해야 정상), 장시간 플레이 시 메모리 증가 둔화 체감은 Profiler로 | 미조치·보고 항목: SummonAttack의 공격 틱당 FindObjectsByType(주기적이라 허용, 적 레지스트리 도입은 후속), BountyBoard OnGUI의 근접 시 StringBuilder(근접 한정이라 허용), 정적 배칭은 SRP Batcher가 대체하므로 미적용(거대 메시 복제 메모리 역효과), URP 그림자 거리·품질 설정은 룩 영역이라 미조정. 에디터 측정치(참고): Mono 힙 1.65GB는 에디터 오버헤드 포함 — 빌드 기준 아님 |
| 2026-07-27 | **대동여지도풍 여지도(輿地圖) 화면(사용자 결정: 맵은 「지도 화면 먼저, 실제 지형은 다음」).** ① Tools/recraft_generate_map.py — 대동여지도풍(산줄기 연첩·목판 먹선·한지) 세로 지도 2안 생성: A안=먹 실루엣 강토+도로망 / **B안=산줄기 목판 표현(대동여지도 특징에 더 가까워 기본 배선 [가정] — 최종 선택은 사람)**. ② Presentation/MapUIController — M키 토글 [가정], 코드 조립 UGUI(딤+세로 지도 한 장+힌트, sortingOrder 42=서책 위). **플레이어 위치 표시 없음** — 미니맵 금지 정책과 결 맞는 순수 디제틱 소품. ③ EC_MapOpened(Bool) 채널 + CodexCursorGlue 두 번째 인스턴스(지도 채널 구독)로 커서/시점 처리. 씬 저장 | 실플레이(사람): M키 지도 손맛, **A/B안 선택**(교체는 MapUIController._mapSprite), 생성 아트의 가짜 글자·나침반(B) 허용 여부 — 거슬리면 재생성 또는 인페인팅 | **여지도 화면 전체(지도 아트 A/B·MapUIController 코드 조립 UI)는 임시(사용자 지시 2026-07-27) — 추후 정식 교체 예정.** 정식은 강토 레이아웃 v0.2 지명·지형과 일치하는 커스텀 지도 필요(생성 아트는 실제 지리 무관). 대동여지도(1861)는 시대상 후대지만 대체역사 미술 레퍼런스로 사용(사용자 결정). 서책·지도 동시 열림 처리 안 함(프로토 허용). **사후 수정(사용자 신고 「맵 안 나오는데」): 스크립트로 textureType=Sprite만 설정하면 spriteImportMode가 None으로 남아 Sprite 서브에셋이 안 생긴다 — 반드시 spriteImportMode=Single까지 명시할 것.** 같은 함정으로 null이던 서책 패널·낱장 아이콘·습득물 폴백 10개도 일괄 재배선(이전 세션 잠복 버그) |
| 2026-07-27 | **서책 UI 3D 낱장 뷰어 + 음양오행 고서 4종 확장(사용자 요청: 왼쪽=한문 원문 낱장 3D(드래그 회전·휠 확대)/오른쪽=한글 번역, 조선 초기 유통 원전으로 다양화).** ① Presentation/CodexPageViewer 신설 — 씬 밖 지하(0,-400,0) 숨은 무대 + 전용 레이어 **CodexStage(8, TagManager에 등록·Main/ViewModel 카메라에서 제외)** + 전용 카메라(RenderTexture 1024², 렌더러 1=먹 포스트 없는 뷰모델 렌더러 — 이중 적용 교훈 준수). 낱장은 코드 조립: 한지 판(앞·뒤) + **광곽(붉은 인찰 테두리)** + 장서인 + **세로쓰기 우횡좌서 한문 조판**(HanjaQuote를 12자 열로 분할, TextMesh LegacyRuntime 한자 OS 폴백, 열 초과 시 자동 재배분). 드래그=Orbit(피치 ±75°)·휠=Zoom(0.35~1.3m), CodexPageDragRelay(IDragHandler/IScrollHandler)가 RawImage에서 중계. ② CodexUIController 개편: 좌단 수집 목록(10슬롯) / 가운데 낱장 3D 뷰어(RawImage) / 오른쪽 제목·**出典**·한글 번역+주석, 수집 수 「藏書 n/10」, EventSystem 자동 생성. ③ **고서 4종 추가**(전부 조선 초 실제 유통 원전 — 여말선초 성리학 정통 계보): 계사전 음양(一陰一陽之謂道)·계사전 태극(易有太極 — 태극 진엔딩 복선 문구)·홍범 오행(五行…水曰潤下 — 오행 거동의 원전)·태극도설 생오행(陽變陰合而生水火木金土 — 초성+중성 결합 이치). HaeryePageSO에 SourceBook 필드 추가, 기존 6장에 出典 기입. ④ 습득물 4개 배치(마을 서편/남쪽 들/북동 들판/퍼즐 마당 안) — 총 10개. ⑤ 검증: 에디터 임시 무대 렌더 3회 반복 — 한자 세로 조판 정상 렌더 확인, 카메라 거리 0.62→0.75·FOV 28→40·characterSize 0.005→0.0038로 낱장이 광곽 안에 들어오게 보정. 컴파일 에러 0, 씬 저장. 부수: HaeryePagePickup 라벨에 폰트 머티리얼 수동 배선 추가(TextMesh 글자 안 보이는 함정), Presentation asmdef에 URP 런타임 참조 추가 | 실플레이(사람): I키 → 낱장 드래그 회전·휠 줌 손맛, 원문 조판·한글 번역 대조 **감수(사람)** — 특히 태극 복선 문구 톤. 새 고서 4곳 습득 동선 | **세계관 전제**: 해례본이 조선 초에 존재하는 것 자체가 이 대체역사의 근간(작도의 원전) — 나머지 4종은 실제 여말선초 유통 원전이라 시대 정합. HaeryePageSO 클래스명은 해례본 한정 시절 잔재(후속 정리 후보). 파괴적 아님: 기존 6장 SO·습득물 그대로, UI만 개편. **낱장 3D(코드 조립 한지판+조판)와 SM_HaeryeBook 고서 모델은 임시 스탠드인(사용자 지시 2026-07-27) — 추후 정식 모델링으로 교체 예정, 교체 시 CodexPageViewer.BuildLeaf와 P_HaeryeBook 프리팹만 갈아끼우면 됨** |
| 2026-07-27 | **해례본 습득물 3D 전환 — 2D 스프라이트 → Meshy 고서 모델 + 부유 원문 라벨(사용자 요청: "고서를 3D 모델로, 원문 텍스트도 모델에 뜨게").** ① 모델 소싱: KCISA에 책 메시 없음 확인 → **Meshy image-to-3d에 Recraft 고서 아이콘(T_UI_HaeryeBook)을 입력**(Tools/meshy_generate_book.py, 6k폴리) — 2D UI 아이콘과 3D 습득물의 룩이 같은 원화에서 나와 일치. 결과물 SM_HaeryeBook: 선장본(오침안정법) 형태·표지 제첨·능화 문양·책배 종이결까지 재현(컴포짓 렌더로 확인). ② HaeryePagePickup 재작성: _modelPrefab 3D 모델(0.45m [가정] 균등 정규화·바운드 중심 정렬·부유·회전) + **TextMesh 원문 라벨**(Title+HanjaQuote, LegacyRuntime.ttf 동적 폰트 — 한자는 OS 폴백, 요 빌보드로 카메라 향함, 9m [가정] 거리 페이드) — 스프라이트는 미배선 폴백으로 존치. ③ M_HaeryeBook(ToonLit·_Saturation=1 채색 — 수집품은 플레이어 관심 대상 [가정]), P_HaeryeBook 프리팹(콜라이더 제거 — 습득은 거리 판정), 습득물 6개 전부 배선, 씬 저장, 컴파일 에러 0 | 실플레이(사람): 3D 고서 크기감(0.45m)·회전 속도, **원문 라벨 한자 렌더링**(LegacyRuntime 동적 폰트의 OS 폴백은 에디터 렌더로 미검증 — 실행 시 확인 필수), 라벨 가독 거리(9m), Wolin 폰트로 교체 여부(현재 라벨은 기본 폰트 — Wolin에 한자 글리프가 없을 수 있어 보류) | 프리팹 에셋 직접 screenshot-isolated는 빈 렌더가 나옴 — 씬에 임시 인스턴스를 놓고 찍을 것(BookTest 패턴, 촬영 후 삭제). 컨셉아트→Meshy 교훈의 연장: 2D 아이콘이 이미 있으면 그걸 image-to-3d 입력으로 재사용해 2D/3D 룩을 묶는다 |
| 2026-07-27 | **훈민정음 해례본 수집 시스템 + 서책 인벤토리 UI(사용자 결정 — 제자해의 음양오행×창제원리를 세계관 강화 수집 요소로).** 부수: 앵커 표식 글자 크기 28→0.7m 수정(AttachLetter 파라미터가 월드 높이(m)였음). ① Data/HaeryePageSO — 낱장 데이터(제목·관련 오행·한문 원문·한글 풀이·세계관 주석). **제자해 6장**: 아음ㄱ=木(牙錯而長)·설음ㄴ=火(舌銳而動)·순음ㅁ=土(唇方而合)·치음ㅅ=金(齒剛而斷)·후음ㅇ=水(喉邃而潤)·중성 천지인(ㆍ天圓·ㅡ地平·ㅣ人立, ㅏㅗ=양·ㅓㅜ=음) — 실제 해례본 구절 기반, **게임 작도 체계의 원전 역할**(각 장 Lore가 작도 규칙과 연결). 의역 [가정] — 감수는 사람. ② Combat/CodexInventory(플레이어, HashSet+OnChanged) · Combat/HaeryePagePickup(E 습득, SpriteRenderer 고서 비주얼 부유·회전, CargoPickup 패턴). ③ Presentation/CodexUIController — I키 토글 [가정], 코드 조립 UGUI: 펼친 고서(한지) 패널 + 좌측 수집 목록 6슬롯(미수집=?? 흐림) + 우측 한문·풀이·주석, 수집 수 표기(訓民正音 解例 n/6), Wolin 폰트. ④ ProtoGlue/CodexCursorGlue — EC_CodexOpened(Bool) 구독해 커서 해제·시점/이동 정지(작도 모드 글루와 같은 결). ⑤ **Recraft 3종 생성**(Tools/recraft_generate_codex.py — 기존 ui 스크립트 복제): T_UI_CodexPanel(펼친 고서 스프레드)·T_UI_HaeryeBook(오침안정법 고서 아이콘)·T_UI_HaeryePageIcon(낱장) — Sprite 임포트 설정. ⑥ 습득물 6개 배치: 마을·배달소·차 옆·마당 입구 + **퍼즐 보상 2곳**(돌다리 건너 절벽 B 위=치음이 아닌 순음? 배치: 절벽B=순음(토), 도약탑 꼭대기=치음(금)) — 새 필드 술식을 쓸 동기 부여. 컴파일 에러 0, 씬 저장 | 실플레이(사람): 습득→I키 서책→낱장 열람 흐름, 패널 레이아웃(코드 조립이라 해상도별 확인), **제자해 본문 감수(사람)** — 실제 원문 대조 | 저장 미연동(세션 한정) — ISaveService 연동은 후속. 수집 완성 보상(예: 6장 완성 시 특전)은 기획 미결로 남김. 탑승 중 I키 UI 겹침 미검증 |
| 2026-07-27 | **퍼즐 마당 별도 공간 분리(사용자 신고: 돌다리 지형이 보스방과 겹침).** 점유 조사로 확인 — 퍼즐 지형(46,5~87,48)이 CombatRooms(-43,-14~69,44)와 대면적 중첩. 남동 코너가 공터임을 확인하고 PuzzleTestGround 루트 이동(자식 절벽·경사로·계단·앵커·표식 전부 함께)으로 (33,-73~80,-24)에 재배치 — 마을(-77~1)·차(-12,-34)·보스방과 전부 무중첩. 「별도 공간」답게 높이 2m 경계 담장 4면 + 북쪽 입구 8m 개방(PuzzleYardWalls) 추가. 부감 렌더로 보스방과의 분리 확인, 씬 저장 | 실플레이(사람): 북쪽 입구로 진입 동선, 표식 글자(TextMesh 크기 28 [가정])가 플레이어 시점에서 적당한지 | 앵커·표식이 루트 자식이라 루트 한 번 이동으로 전부 정합 유지됨 — 향후 퍼즐 구역 재배치도 루트 이동으로 |
| 2026-07-27 | **필드 앵커 시스템 — 설치형은 지정 위치에서만, 밖에서는 먹이 흩어지는 실패 연출(사용자 제안 채택: 설치형만 제약·실패 시 먹 50% 환급).** ① S_ToonLitTemp에 _MaterializeErode 추가 — clip()으로 노이즈를 따라 픽셀을 버려 먹이 흩어져 사라지는 침식. ② InkMaterializer에 PlayFailure(먹물 유지 0.5s → 침식 0.7s [가정]) 모드. ③ FieldSpellAnchor(Combat 신설): (오행,받침)·수용 반경 지정, **앵커 트랜스폼=설치물 스폰 자리**(레벨 저작 — 다리가 틈에 정확히 걸침, 기즈모 표시). ④ FieldSpellExecutor: 설치형(뭄·굼)은 반경 내 일치 앵커 탐색 — 성공 시 앵커 위치에 실체화, 실패 시 먹물 형상(판정 없음)이 맺혔다 흩어짐 + PlayerBuffLookup.RefundInk로 먹 InkCost×0.5 환급 + 로그. 굼 실패 시 도약도 발동 안 함. ⑤ 돌다리 예시 지형 완성: 절벽 A로 오르는 경사로(22도) 추가 — 기존 절벽 틈이 실제 플레이 가능해짐. 앵커 4개(돌다리 1 — 틈 중앙 상공 / 도약 3 — 지면·계단 1·2단) + **권장 위치 표식 4개**(바닥 먹 문양 원반 + 글자 「뭄」/「굼」 — 프로토 디제틱, 정식 문양 데칼은 아트 단계). ⑥ **부수 발견·수정: 근경 산자락이 플레이 필드 침범** — Mtn_Peak_NE1(56,54)의 산기슭이 동쪽 필드·보스 구역을 통째로 덮고 있었고(플레이어 시점 배경으로만 보여 그간 미발견), Mtn_Ridge_E2(133,6)도 퍼즐 지형 침범 → 각각 (120,110)·(195,0)으로 이동해 필드 확보. 부감 렌더로 확인 | 실플레이(사람): 표식 위 성공/밖 실패 연출 대비, 실패 먹물 형상이 정보(어떤 설치물이었는지)를 주는지, 환급 체감. 산 이동 후 플레이어 시점 원경 재확인 | **주의**: 원산들엔 콜라이더가 없어 걸어서 산 비주얼 안까지 들어갈 수 있음(프로토 허용 — 후속에서 접근 차단 지형 또는 콜라이더 필요). 근경 산 배치 시 산기슭 footprint가 플레이 공간을 침범하는지 부감으로 확인하는 절차를 표준화할 것 |
| 2026-07-27 | **먹 실체화 연출 + KCISA 설치물 모델(배정표 확정 — 사용자 승인, 콜라이더 즉시 기본값).** 필드 설치물이 "먹물로 생겨나 실체로 굳는" 공통 연출 도입. ① S_ToonLitTemp에 _MaterializeProgress(0=먹/1=실체, 기본 1 — 기존 머티리얼 무영향: 미직렬화 프로퍼티는 셰이더 기본값 폴백을 역이용)·_MaterializeInkColor·_MaterializeNoiseScale 추가 — 월드 노이즈(InkFbm3 재활용) 얼룩을 따라 먹이 마르듯 실체가 차오르고 경계는 먹빛 번짐. ② InkMaterializer(Combat — 처음 Presentation에 뒀다가 asmdef 의존 방향(Combat←Presentation) 위반 발견, Combat으로 이동): MPB로 진행도 0→1, FieldSpellConfig.MaterializeSeconds 1.2s [가정]. 판정(BoxCollider)은 시전 즉시 유효 — 연출만 지연(사용자 결정). ③ 설치물 모델 소싱 규칙(사용자 결정): KCISA 우선→없으면 Meshy. 감사 결과 둘 다 KCISA로 충당: 돌다리=KT_SplineBuilder SM_CW_Stone(장대석 7.8m) **2개 나란히**(농다리식, 폭 ~2.6m=판정과 일치)·기둥=SM_P_Wood_1(통나무 5m). P_Field_StoneBridge/P_Field_WoodPillar 프리팹 조립(원본 텍스처 승계한 ToonLit 머티리얼 M_Field_Stone/Wood, _Saturation=0, 자체 콜라이더 제거 — 판정은 실행부 담당), FieldSpellConfig에 배선. ④ FieldSpellExecutor 재구성: 판정 루트+비주얼 자식 분리, 프리팹은 균등 스케일 정규화(Meshy/KCISA 축 문제 회피), 폴백 프리미티브 유지. ⑤ 검증: 진행도 0.35 고정 스폰 렌더로 먹→돌 중간 상태(먹빛 덩어리에 돌 질감 얼룩 발현) 확인, 컴파일 에러 0, 정리 후 씬 저장 | 실플레이(사람): 뭄 시전 실체화 속도감(1.2s — 빠르게/느리게는 FieldSpellConfig.MaterializeSeconds), 장대석 2열 돌다리 폭 체감, 굼 통나무 연출 | 배정표는 매핑계획 v0.1대로 확정(사용자). 실체화는 설치형 공통 문법 — 2차 구현(철책 숨·디딤돌 멈 등)도 InkMaterializer 붙이면 됨. KCISA 메시 재질은 원본 베이스 텍스처를 ToonLit로 승계해 수묵 톤 램프·실체화 모두 적용됨 |
| 2026-07-27 | **필드/퍼즐 작도 도입 — 「음(陰)+받침=필드」 블록 규칙으로 45자 전환, 4종 구현(사용자 결정: 전투 작도 과다 축소 목적, 중성 추가 금지).** 기획 확정 과정: ㅡ/ㅣ 특수 중성 축 제안 → 기각(기존 글자 재배정 선호) → ㅜ+ㅁ 4자 제안 → "더 많이, 음양오행에 맞게 전부 매핑" 지시 → **음성(ㅓ/ㅜ)+받침 45자 통전환 확정**(전투 120→75자, 버프 소환 ㅜ+ㄱ 5자는 소환 유지). 매핑 원리: 초성=오행 재료 / ㅓ=자신·단일에 걸기 / ㅜ=세상·영역에 놓기 / 받침=거동 유추(ㄱ붙듦·ㄴ지속·ㅁ설치·ㅅ뚫음·ㅇ이음) — 45자 전부 CSV에 기술(41자는 "필드 예약·미구현" 표기). ① DiagramCategory.Field 추가, SpellCaster에 Field 분기 → FieldSpellExecutor(Combat 신설). ② 구현 4종: 뭄(토ㅜㅁ)=전방 9m 돌다리 설치(25s [가정]) / 굼(목ㅜㅁ)=도약(초속 12m/s≈7.3m, EC_PlayerLeap Float 채널→PlayerLeapGlue가 FPC._verticalVelocity 리플렉션 주입) / 언(수ㅓㄴ)=낙사 방어 수막 12s / 넌(화ㅓㄴ)=사거리 12m 내 마석 자동차 화력 가속 x1.8·10s(VehicleController.ApplyBoost 신설). ③ **낙사 피해 시스템 신설**(언의 전제): PlayerFallDamage — CharacterController 착지 시 최대 낙하 속도가 SafeFallSpeed(12m/s) 초과분×6 피해, 수막이면 무효. 수치 전부 FieldSpellConfigSO [가정]. ④ FloatEventChannelSO(Core) 신설. ⑤ **Vehicle asmdef 신설** — Scripts/Vehicle에 asmdef가 없어 Assembly-CSharp 소속이었고 Combat asmdef가 참조 불가(컴파일 에러로 발견, 이전 글롭이 MandateOfInk.*.asmdef 명명을 놓쳤음): MandateOfInk.Vehicle.asmdef(Core·Data 참조) 생성, Combat asmdef에 참조 추가. ⑥ CSV 45행 python 갱신 → 기존 SpellDiagramCsvImporter 메뉴 실행으로 SD 120개 멱등 동기화(CSV=단일 진실 원칙 그대로). ⑦ 씬: FieldSpellConfig·EC_PlayerLeap 에셋, FieldSpellExecutor(SpellCaster 옆)·PlayerFallDamage·PlayerLeapGlue(플레이어) 배선, 퍼즐 테스트 지형 5개(절벽 틈 8m — 다리 9m로 잇기 / 도약 계단 3단 5m 단차 / 최상단 15m — 낙사 테스트) | 실플레이(사람): 절벽에서 뭄 긋고 건너기, 굼으로 계단 오르기, 15m에서 언 없이(낙사 피해)·언 걸고(무효) 뛰어내리기, 차 옆에서 넌 긋고 주행. 45자 매핑 표 검토 — 수정 지시 주면 CSV만 고치고 임포터 재실행 | 미구현 41자는 시전 시 "필드 예약" 로그+옅은 퍼프(인식·시전 파이프라인은 통과). 전투 손실: 음성 방어(거/너/머/서/어·구/누/무/수/우 무받침)와 패링은 유지되나 음성+받침 부가효과 조합은 소멸 — 이전 세션의 음성 행 받침 차별화 서술은 Field로 대체됨(양성 행은 전부 유지). 격발(상합)은 양성 ㅁ받침(감/곰 등)에 그대로 존속 |
| 2026-07-27 | **마석 자동차 주행 전복 수정 + 재조립(사용자 신고: D 조향 시 차가 뒤로 넘어감, 차가 사람 대비 작음).** 원인: FBX 루트를 그대로 물리 루트로 썼는데 Meshy 축 보정 회전이 박혀 있어 ① `rb.rotation × yaw`(오른쪽 곱=로컬 축) 조향이 월드 기준으로는 차를 옆으로 굴리는 회전이 됨 ② FreezeRotation 제약도 월드축 기준이라 못 막음. **수정 2단**: ① VehicleController 조향을 왼쪽 곱(`Quaternion.Euler(0,yaw,0) × rb.rotation`) 월드 Y축 기준으로. ② 차를 재조립 — **깨끗한 빈 루트(순수 요 회전·스케일 1) + FBX 모델 자식** 구조로: 물리(BoxCollider·Rigidbody)·VehicleController·상호작용·글루·Seat 전부 루트에, 축 보정·임포트 스케일은 Model 자식에 격리. 모델 길이축(X)을 루트 전방(+Z)에 FRONT_YAW_FIX=90도로 정렬 — 루트 전방 9m 임시 카메라 렌더로 마석 보일러(차 정면)가 정면에 보임을 확인해 부호 확정. 크기 4.5→6.0m [가정](사용자 피드백: 사람 대비 비율). 좌석은 전방 15%·중심 위 5% [가정] | 실플레이 재검증(사람): 조향 전복 해소·크기감·좌석 위치. 하차 위치(차 왼쪽 2.2m)도 확인 | **핵심 교훈**: Meshy FBX를 차량·기계처럼 물리로 움직일 오브젝트로 쓸 땐 FBX를 직접 루트로 쓰지 말고 반드시 빈 루트(정렬 축) 아래 자식으로 넣을 것 — 축 보정 회전이 물리 회전·제약·forward를 전부 오염시킨다. 전방 축은 렌더 확인(전방에서 바라본 임시 카메라)으로 실측 확정하는 것이 안전 |
| 2026-07-27 | **실플레이 "화면 흐림 너무 강함" 근본 원인 규명·수정 — 먹 포스트가 뷰모델 오버레이 카메라에서 이중 적용.** 사용자 신고: 게임뷰만 심하게 뿌옇고 씬뷰는 정상. 1차엔 톤 파라미터(세피아·PaperPoint·벽 밝기) 조정을 시도했으나 스크린샷 비교로 효과 없음을 확인 — 이 과정에서 **셰이더 Properties 기본값 변경은 이미 저장된 머티리얼에 반영되지 않음**(머티리얼이 생성/저장 시점 값을 통째로 직렬화)을 실측으로 확인, 일괄 변경은 반드시 SetFloat 명시 스윕으로. 2차 진단에서 진범 발견: URP 렌더러가 1개뿐이라 InkPostTempFeature(풀스크린 먹 포스트)가 베이스 카메라와 **ViewModelCamera(오버레이, 붓 전용)** 양쪽에서 실행 — 오버레이 카메라의 씬 뎁스는 붓밖에 없어 화면 대부분이 무한 거리로 읽히고, 원경 종이 페이드가 화면 전체를 종이로 덮음(근경 사물까지 유령처럼 씻긴 증상과 일치. 에디터 씬뷰·screenshot-camera 단일 렌더에는 오버레이 패스가 없어 정상으로 보였던 것). **수정**: URP_Renderer 복제 → 피처 제거한 URP_Renderer_ViewModel 생성, 파이프라인 렌더러 리스트 index 1로 추가, ViewModelCamera에 SetRenderer(1) — 이제 먹 포스트는 베이스 카메라에서 1회만. 1차에서 임시로 낮췄던 톤 값들은 근본 원인이 밝혀졌으므로 승인 기준값으로 전부 원복(세피아 0.35·그레인 0.10·환경 76개 PaperPoint 0.68/HighlightBoost 0.5·ProtoWall 벽색 git 원본 0.42/0.4/0.38) | 실플레이 재검증(사람): 화면 흐림 해소 확인. **"주행 잘못됐고"의 구체 증상 미청취 — 확인 필요**(카메라 위치? 조향? 차가 안 움직임?) | **핵심 교훈 2건**: ① 카메라 스택(베이스+오버레이) 환경에서 풀스크린 렌더러 피처는 오버레이에도 걸린다 — 뎁스 의존 포스트는 오버레이 전용 렌더러(피처 없는)로 분리할 것. 원경 페이드 도입 전에는 이중 적용 부작용이 세피아·그레인 2회 정도라 티가 안 났지만, 뎁스 기반 페이드가 들어가며 파국적으로 드러남. ② 룩 이상 신고 시 "씬뷰는 정상"이라는 단서 = 포스트/카메라 계층 문제 신호 — 파라미터 튜닝보다 렌더 경로 진단 먼저 |
| 2026-07-27 | **마석 자동차 — 컨셉아트 기반 모델 생성 + 탑승/주행 시스템(사용자 결정: 퍼즐 작도보다 차 먼저 — 의존성·코어 골격·배달 루프 연결 근거).** 배치 전 나무 13그루(소나무·버드나무) 제거(사용자 요청, 풀덤불 28은 유지·에셋은 보존). ① **Meshy image-to-3d 첫 사용**: 사용자 삼면도 컨셉아트(바탕화면 오행부 프로젝트/컨셉아트/마석 자동차.png)를 PIL로 정면/측면/후면 3분할 → multi-image-to-3d(data URI, 폴리 30k)에 투입(Tools/meshy_generate_car.py, 단일 이미지 폴백 내장) — 결과가 컨셉 충실(한옥 지붕·등롱·청록 발광 마석 바퀴·붉은 패널). 텍스트 생성보다 컨셉 재현이 훨씬 좋음 — 컨셉아트 있는 에셋은 image-to-3d 우선할 것. ② 탑승 시스템 4파일: Data/VehicleConfigSO(속도·가감속·조향·탑승 수치 전부 [가정] 데이터) / Vehicle/VehicleController(명시적 상태머신 Parked↔Driven — CLAUDE.md 탑승↔도보 규칙, Rigidbody 아케이드 주행: 속도 비례 조향·후진 역조향·브레이크 우선, SpeedMultiplier 훅=퍼즐 차량 가속 술식 대비) / Vehicle/VehicleMountInteraction(E 토글 탑승/하차 + OnGUI 프롬프트 — CargoPickup 패턴, BoolEventChannelSO 발신) / ProtoGlue/VehicleMountGlue(채널 구독 — FPC·CharacterController 끄고 플레이어를 Seat에 부착, 탑승 중 마우스 룩은 글루가 CinemachineCameraTarget 직접 회전 요±120/피치±40 [가정], 하차 시 차 왼쪽 2.2m — StarterAssets 직접 참조 금지 규칙 준수). ③ 씬 배선: SM_MagicStoneCar 임포트(useFileScale 끔+실측 교훈), 길이 4.5m 정규화, 마을 근처(-12,-34) 배치, BoxCollider(실측 핏)+Rigidbody(600kg, 회전 XZ 동결), M_MagicStoneCar(S_ToonLitTemp, **_Saturation=1 채색 — 차는 플레이어측**), VC_MagicStoneCar 설정 SO, EC_VehicleMounted 채널(Data/Events), SerializedObject로 전 필드 배선. 컴파일 에러 0, Composite 렌더로 모델 확인, 씬 저장 | 실플레이 검증(사람): E 탑승→WASD 주행→마우스 룩→E 하차. 예상 조정 지점: Seat 위치(현재 차체 중앙 위 [가정] — 어색하면 Seat 자식 이동), 주행 수치(VC_MagicStoneCar), 탑승 중 작도(Q)·전투 입력이 살아있는 문제(미처리 — 추후 상태머신에서 배제 여부 결정 필요) | 미결(다음 논의): 탑승 중 작도 허용 여부, 마석 연료 소비 여부, 화물 적재 연동(현 PlayerCargo는 도보 기준), 탑승 전투(기획서의 탑승↔도보 전투 모드). 퍼즐 작도 4종은 차 완성 후 착수(사용자 합의된 순서) |
| 2026-07-27 | **Meshy 식생 3종 생성 + 41개 배치 — 소나무·버드나무·풀덤불(사용자 요청, 원산과 같은 파이프라인).** ① Tools/meshy_generate_vegetation.py 신설 — SM_Tree_Pine(비틀린 줄기·층진 수관, 인왕제색도풍 소나무)·SM_Tree_Willow(늘어진 가지)·SM_Grass_Tuft(풀덤불) 3종 생성(~25분). ② 임포트(useFileScale 끔) 후 원산 교훈 그대로: 임시 인스턴스 실측 → baseScale 곱·baseRot 합성 → 목표 높이 역산(Meshy 원본은 셋 다 ~190유닛). ③ M_Tree_Pine(0.55)/M_Tree_Willow(0.65)/M_Grass_Tuft(0.8) — S_ToonLitTemp+_Saturation=0, BaseColor로 먹 농담 차등 [가정]. ④ Vegetation 루트 아래: 소나무 10(높이 8~12m, 마을 가장자리·들판·동서 변두리)·버드나무 3(집 곁 — 그림 구도, 7~9m)·풀덤불 28(고정 시드 42 산포, 높이 0.7~1.2m, 플레이어 스폰 14m·마을 중심 10m 회피, 그림자 캐스팅 끔). ⑤ 검증 중 **Cinemachine 함정 발견**: MainCamera transform을 스크립트로 바꿔도 에디터에서 Cinemachine 브레인이 즉시 되돌림 — 이전 검증 샷들도 실제론 전부 스폰 방향이었음. 임시 카메라(TempShotCam) 생성으로 우회해 각도 자유 촬영 → 부감 전경(마을+먹빛 산 겹침)·서쪽 소나무 근접(비틀린 줄기+층진 수관 실루엣 — 그림 속 소나무 그대로) 확인 후 임시 카메라 삭제. 단 임시 카메라엔 InkPost 풀스크린 피처가 안 걸려 원경 여백은 플레이어 카메라 기준으로만 유효 확인. 씬 저장 | 실플레이로 식생 배치 밀도·위치 확인(사람) — 나무 개별 이동은 Vegetation 아래서, 진하기는 M_Tree_* _BaseColor | **신규 교훈**: 에디터에서 씬 카메라 연출 검증 시 MainCamera는 Cinemachine이 소유 — transform 직접 조작 무효, 임시 카메라를 만들어 찍고 지울 것. 향후 각도 검증 표준 절차로 사용 |
| 2026-07-27 | **Meshy 원산(遠山) 2종 생성 + 플레이어 기준 3겹 9개 배치 — 인왕제색도 원경 구도 완성(사용자 요청).** ① 키 복구: Tools/.env가 사라진 상태였고 사용자가 API.env로 생성 → .env로 복사(스크립트 호환) 후 **API.env는 gitignore 미적용이라 삭제**(키 커밋 방지 절대 규칙), 사용자가 .env.example을 개명한 것이어서 템플릿은 git에서 복원. ② Tools/meshy_generate_mountains.py 신설(기존 brush 스크립트 패턴) — SM_Mountain_Peak(인왕산풍 화강암 주봉, 190×112m)·SM_Mountain_Ridge(낮은 다봉 능선, 190×29m) 2종 preview→refine→FBX+텍스처, 백그라운드 실행 약 8분. ③ 임포트: useFileScale 끔(기존 교훈). **신규 교훈 2건**: Meshy FBX는 루트 노드에 임포트 스케일(×100)·축 보정 회전이 박혀 있어 localScale/rotation을 덮어쓰면 산이 1/100 크기가 되거나 옆으로 눕는다 — 반드시 임시 인스턴스로 월드 바운드·baseScale·baseRot을 실측한 뒤 **곱하고 합성**할 것(에셋 프리팹에서 잰 bounds는 씬 배치와 다를 수 있음). ④ M_Mountain_Peak/Ridge 머티리얼(S_ToonLitTemp, _Saturation=0) 생성, 그림자 캐스팅 끔(원경 배경). ⑤ DistantMountains 루트 아래 9개 배치: 근경 3개(~100m, 폭 85~105m)·중경 3개(~160m, 폭 150~180m)·원경 3개(~240m, 폭 255~275m, 멀수록 크게) — 목표 폭에서 스케일 역산, 회전 다양화. ⑥ 튜닝: 첫 렌더에서 산이 전부 종이로 씻겨 나감 → 산 _BaseColor 0.62→0.42(톤 램프에서 중간 먹 덩어리로 읽히게), M_InkPostTemp 페이드 90~200m·_SilhouetteKeep 0.55로 조정 → 플레이어 시점 렌더에서 근경 능선(디테일 보임)/중경/원경 주봉(씻긴 실루엣) 3단 깊이 확인. 카메라 원복·씬 저장 | 실플레이로 사방 원산 확인(사람) — 특정 산이 너무 크거나 방향이 어색하면 DistantMountains 아래 개별 이동/회전, 진하기는 M_Mountain_* _BaseColor | Meshy 신규 교훈은 위 ③ — 다음 Meshy 모델 배치 때 반드시 참조. 배치 스크립트는 scratchpad(세션 임시)에만 있음 — 재배치 필요 시 PlaceMountains2 패턴(임시 인스턴스 실측→스케일 역산) 재작성 |
| 2026-07-27 | **원경 여백 피드백 반영 — 페이드 거리 후퇴 + 원산 실루엣 보존.** 실플레이 결과 "원경이 너무 빨리 사라지고, 완전히 사라지지 말고 실루엣은 남아야"(인왕제색도의 원산처럼) — 두 가지 수정: ① _PaperFadeStart 15→50m, _PaperFadeEnd 60→160m(슬라이더 상한도 200/500으로 확장) — 아주 먼 것만 여백에 잠김. ② 실루엣 보존: 페이드 세기에 밝기 가중치를 곱함 — `fadeAmount = fade × strength × (1 - _SilhouetteKeep × darkness)`. 밝은 픽셀(하늘·밝은 면)은 종이로 완전히 사라지고, 어두운 픽셀(먹 덩어리)은 _SilhouetteKeep(기본 0.45) 비율만큼 덜 사라져 씻긴 실루엣으로 남는다 — 하늘은 밝아서 자동으로 완전한 여백 유지. CLI로 컴파일(에러 0)·원거리 재렌더 검증: 이전 샷에선 마을이 통째로 사라졌으나 수정 후 같은 거리에서 밝은 바닥은 종이로 녹고 어두운 건물들이 회먹색 실루엣으로 남음 — 레퍼런스의 원산 처리 재현 확인 | 실플레이 재확인(사람) — 실루엣 진하기는 _SilhouetteKeep, 거리는 _PaperFadeStart/End로 튜닝 | 실루엣 보존을 밝기 가중치 하나로 처리한 덕에 하늘/어두운 산 구분 로직이 따로 필요 없음(하늘=밝음=완전 페이드가 자동으로 성립) |
| 2026-07-27 | **환경 룩 최종 방향 확정 — 인왕제색도(정선) 레퍼런스 "여백 있는 수묵" 톤 램프.** 사용자가 인왕제색도를 제시하며 "완전 흑백이 아니라 여백이 있는 느낌"으로 방향 정정 — 핵심 분석: 이 그림은 밝은 곳이 회색이 아니라 **칠하지 않은 한지 바탕(여백)**이고, 어두운 곳에만 먹이 쌓이며, 안개·하늘은 아예 그리지 않은 여백. 즉 채도 제거가 아니라 「밝기→먹-종이 톤 램프 재매핑」이 필요. ① S_ToonLitTemp의 _Saturation=0 거동을 단순 흑백(luma 회색)에서 톤 램프로 교체: `tone=smoothstep(_InkPoint, _PaperPoint, luma)`로 `_InkToneColor`(먹)↔`_PaperToneColor`(한지) 사이를 매핑, _PaperPoint(기본 0.68) 이상 밝기는 순수 종이색으로 눌러 넓은 여백 생성, _TintRetain(기본 0.12)으로 옅은 담채 기운만 잔류 — **기존 환경 71개(_Saturation=0)에 자동 적용**(머티리얼 재배선 불필요), 캐릭터(_Saturation=1)는 무영향. ② S_InkPostTemp에 원경 여백 추가: 뎁스 기반으로 멀수록 _PaperColor로 녹아드는 페이드(_PaperFadeStart 15m/_PaperFadeEnd 60m/_PaperFadeStrength 0.9 [가정]) — 하늘(무한 깊이)은 자동으로 완전한 종이 여백이 되고, 원경의 먹선(외곽선)도 함께 옅어져 원산의 흐릿한 윤곽 효과. ③ 검증: Unity MCP 재연결이 세션에 반영 안 돼 **unity-mcp-cli(CLI) 경로로 우회** — assets-refresh·console(에러 0)·screenshot-isolated 실행, base64 응답을 PowerShell로 디코드해 확인. 마을 전체 샷(원거리)은 거의 전부 종이로 녹음(페이드 동작 증거), 안채(Anchae) 근접 샷에서 지붕=먹덩어리/벽·마당=옅은 톤/뒷담·배경=한지 여백의 3단 구성 확인 — 레퍼런스 방향 재현됨 | 실플레이로 여백 넓이(_PaperPoint)·담채(_TintRetain)·원경 거리(_PaperFadeStart/End) 손맛 튜닝(사람). 밴디식 잉크 코팅(직전 작업)은 이 방향과 결이 달라 기본값 0으로 잠재움 — "잉크 코팅 적용" 메뉴를 실행했다면 "해제" 메뉴로 되돌릴 것 | **아트 방향 변천 기록**: 흑백(2026-07-24) → 밴디 잉크 코팅(같은 날, 미적용 상태로 보류) → 인왕제색도 여백 수묵(2026-07-27, 확정). 잉크 코팅 프로퍼티·InkCoatApplier 메뉴는 코드에 남아있으나 전부 기본값 0(비활성) — 필요 시 재활용 가능. MCP 서버가 세션 도중 끊기면 스킬(unity-mcp-cli run-tool) 경로로 모든 Unity 도구를 동일하게 쓸 수 있음(스크린샷은 base64 디코드 필요) — 향후 세션 참고 |
| 2026-07-24 | **잉크 코팅 셰이더 확장 — 환경 오브젝트를 "잉크에 적셔진 그림"처럼(밴디와 잉크 기계 레퍼런스, 사용자 요청).** 사용자 질문 "오브젝트를 새로 만들어야 하나?"에 대한 분석: 밴디 룩은 지오메트리가 아니라 ①세피아 단색조(기존 먹 포스트·흑백화와 동일 방향) ②손그림 스케치 텍스처 ③젖은 잉크 광택(스페큘러) ④흘러내리는 잉크 자국 — 즉 재제작 불필요, 표면(셰이더/텍스처) 레이어 문제. 구현 옵션 A(셰이더 확장)/B(Recraft 텍스처 재작성)/A+B 제시 → 사용자 "A부터" 선택. ① S_ToonLitTemp에 잉크 코팅 3겹 마스크 추가: _InkCoatBase(전체 착색)+_InkCoatTop(윗면일수록 진하게 — 위에서 부은 잉크, saturate(n.y))+_InkDripAmount(옆면 세로 줄무늬 — xz만 쓰는 ValueNoise라 y방향으로 이어지고, y를 0.35 배율로 섞은 컷 노이즈로 줄기가 중간에 끊김). 합산 coat로 _InkCoatColor에 lerp. ② 젖은 광택: Blinn-Phong 하프벡터 스페큘러를 smoothstep으로 뚝 끊어(셀 룩과 결 맞춤) coat 덮인 부위에만 가산 — "마르지 않은 잉크" 인상의 핵심. ③ **신규 프로퍼티 기본값 전부 0(효과 없음)** — 같은 셰이더를 쓰는 캐릭터/적 43개에 영향 없도록. ④ 환경 적용은 에디터 메뉴로: EditorTools/InkCoatApplier.cs 신설 — 「MandateOfInk/잉크 코팅 적용·해제 (환경 머티리얼)」 메뉴, EnvironmentFolders 배열(현재 House Of Changwon)의 S_ToonLitTemp 머티리얼에만 [가정] 세팅(Base 0.35·Top 0.7·Drip 0.45·Scale 1.5·Gloss 0.6) 일괄 적용, 멱등·해제 메뉴로 롤백 용이 | **Unity MCP 연결 끊김 상태에서 작업 — 컴파일 미검증.** Unity 에디터 포커스 시 컴파일 확인 → 에러 없으면 메뉴 「MandateOfInk/잉크 코팅 적용」 실행 → 한옥 마을 잉크 룩 확인(사람), 세기는 머티리얼 슬라이더 또는 InkCoatApplier 상수 튜닝. 부족하면 B(Recraft 손그림 텍스처) 진행 | Unity MCP가 세션 도중 끊겨 script-execute/스크린샷 검증 불가 — 대신 에디터 메뉴 스크립트로 사람이 실행하는 경로로 우회(멱등이라 재실행 안전). 잉크 코팅은 밝은 흑백 환경 위에 얹으면 명암 대비가 커지므로 InkPost 세피아와의 조합 톤은 실플레이로 판단 필요 |
| 2026-07-24 | **먹 포스트 몰골 실험 롤백 + 환경=흑백/캐릭터·이펙트=채색 아트 컨셉 착수 + 런타임 채도 전환 컴포넌트 신설.** ① 사용자가 실플레이 후 "먹 포스트(S_InkPostTemp) 테두리 흐리게 만든 건 롤백, 셀 셰이딩(S_ToonLitTemp) 오브젝트 표면은 유지"로 결정 — git에 커밋된 적 없는 파일이라 `git checkout e879ec6 -- <path>`로 최초 커밋 시점(단순 뎁스 소벨 외곽선+세피아+그레인+비네트)까지 복원, M_InkPostTemp.mat 프로퍼티 정상 재매칭 확인. ② 아트 컨셉 확정: 나무·풀·건물 등 환경은 흑백, 적·스킬 이펙트는 채색 유지. S_ToonLitTemp에 `_Saturation`(0=흑백/1=원색, luma로 lerp) 프로퍼티 추가 — `[Tooltip(...)]` 어트리뷰트에 한글 유니코드(가운뎃점 등) 섞으면 ShaderLab 파서가 깨짐을 발견, 일반 주석으로 대체해 해결. ③ 실제 대상 조사 — S_ToonLitTemp를 쓰는 43개는 전부 적/보스/소품/프로토였고, 진짜 나무·한옥 마을(House Of Changwon 71+Skybox/Error 2, KoreanTraditional_SmartMaterials Vol.4 54, KT_SplineBuilder 40 등 총 약 210개)은 서드파티 원본 셰이더(URP Lit/Standard/Autodesk Interactive)를 그대로 쓰고 있어 흑백화 영향권 밖임을 확인 — 사용자 확인 후 "한옥 마을부터 우선 테스트"로 축소 진행(사용자 결정: 나무 각 폴더로 확장할지는 결과 보고 판단). ④ 리스크 선확인: S_ToonLitTemp는 노멀맵을 안 써서 그대로 바꾸면 기와·돌벽 등 표면 디테일이 죽는다는 점을 미리 짚고, 노멀맵 지원(TBN 정접 공간 샘플링, `_BumpMap`/`_BumpScale`)을 먼저 추가한 뒤 71개 변환(원본 _BaseMap/_BumpMap/_BaseColor/tiling/offset을 읽어 그대로 이식 + _Saturation=0 배선) — screenshot-isolated로 실제 렌더 확인, HanokVillage 계층 전부 S_ToonLitTemp 사용 확인(nonToonMatSlots=0), 노멀맵 디테일 유지된 채 흑백 전환됨을 눈으로 검증. ⑤ **런타임 트리거 전환 컴포넌트 신설**(사용자 질문 "인게임에서 다시 채색 트리거 가능한지"에 대한 답으로 구현) — `Presentation/SaturationController`(MaterialPropertyBlock으로 렌더러별 `_Saturation`을 SmoothDamp 보간, 공유 머티리얼 비침습) + `Presentation/SaturationTriggerZone`(트리거 콜라이더, CharacterController 진입/이탈 감지해 대상 SaturationController들에 목표 채도 지시, 편도/왕복 선택 가능) — 기존 BrushInkTint.cs의 MaterialPropertyBlock 패턴 재사용. HanokVillage에 SaturationController 부착·씬 저장 완료(트리거 존 실제 배치는 레벨 디자인 미결) | 나머지 환경 폴더(KT_SplineBuilder·SmartMaterials·StarterAssets 프로토) 흑백 전환 여부 결정, SaturationTriggerZone 실제 씬 배치(연출 지점 정해지면), 실플레이로 몰골 노이즈 셀 경계+채도 전환 손맛 확인 | **git 롤백 교훈**: 이번 세션에서 만든 파일이 커밋된 적 없으면 git으로는 "최초 생성 시점"까지만 되돌아가며, 그사이 여러 버전을 거쳤다면 그 중간 상태로는 못 돌아간다 — 롤백 전 사용자에게 정확한 목표 시점을 확인해야 함(이번엔 "최초 단순 버전"으로 확인받고 진행). 대규모 셰이더 교체(200개+)는 노멀맵 등 슬롯 손실 리스크를 먼저 짚고 범위를 한 폴더로 축소해 검증 후 확장하는 접근이 유효했음 |
| 2026-07-24 | **셀 셰이딩(S_ToonLitTemp.shader)에도 몰골 노이즈 확장 — Dissolvable Ink Painting Effects류 에셋의 "돌" 예시 사진을 사용자가 제시하며 분석 요청.** 정자·바위 스크린샷(오브젝트 실루엣 경계가 또렷한 3D 티)에 이어, 사용자가 해당 에셋의 돌 표면 예시 사진을 가져와 "이건 어떻게 구현한 걸까" 질문. 분석: 경계가 오브젝트 실루엣이 아니라 **표면 셰이딩의 명암 경계**(볼록한 능선 따라 하이라이트가 거의 클리핑, 그 옆에 붓터치 얼룩)에서 생기고, 노이즈로 일그러진 걸로 보아 셀 셰이딩 threshold + 노이즈 조합으로 추정 — 화면 후처리(S_InkPostTemp)가 아니라 오브젝트 머티리얼 셰이더(S_ToonLitTemp, "Flat Kit 구매 시 교체 예정" 스탠드인) 영역임을 짚고 확장 여부 확인 후 진행(사용자 승인). ① 기존 `cel = floor(ndl*steps)/(steps-1)` 계단식 명암 양자화에 월드 좌표 기반 프랙탈 노이즈(Hash3+ValueNoise3+2옥타브 InkFbm3, 시간항 없음 — 정적)를 얹어 `ndl` 자체를 흔듦(_InkCelNoiseScale·_InkCelNoiseStrength) — UV 대신 월드 좌표 사용 이유: 오브젝트가 회전해도 노이즈가 표면에 들러붙은 것처럼 고정되어 보이게(UV 왜곡 있는 프로시저럴 메시에도 안전). ② 계단 경계 전환 구간만 smoothstep으로 풀어(_InkCelSoftness) 칼같은 계단 대신 붓이 스친 그러데이션 밴드로. ③ 최상단 밝기 계단을 순백에 더 가깝게 밀어붙이는 하이라이트 부스트(_InkHighlightBoost) — 레퍼런스의 거의 클리핑된 흰 하이라이트(마른 붓의 비백) 재현. ④ ShadowCaster/DepthOnly 패스는 변경 없음(라이팅 무관 패스라 영향 없음). 컴파일 에러 0, 이 셰이더를 쓰는 머티리얼 43개(보스 팔 부품 등) 전부 신규 프로퍼티 4종이 셰이더 기본값으로 정상 반영됨을 script-execute로 확인 | 실플레이로 셀 경계 노이즈 스케일·세기·소프트니스·하이라이트 부스트 손맛 확인(사람) — 특히 대형 오브젝트(보스 등)와 소형 오브젝트에서 _InkCelNoiseScale이 다르게 느껴질 수 있음(월드 단위 고정 스케일이라 오브젝트 크기에 따라 상대적 무늬 밀도가 달라짐, 필요시 오브젝트별 머티리얼 인스턴스로 조정) | **핵심 교훈**: "테두리를 흐리게"(포스트 프로세싱, 화면 공간)와 "표면 자체가 붓터치처럼 보이게"(셀 셰이딩, 오브젝트 공간)는 서로 다른 레이어 — 사용자가 레퍼런스 사진에서 어떤 느낌을 원하는지 먼저 관찰해 특징을 말로 분석하고, 어느 레이어(포스트 vs 머티리얼)에서 구현되는 효과인지 짚은 뒤 범위 확장 여부를 확인하는 순서가 반복적으로 유효했음. 두 셰이더(S_InkPostTemp=화면 후처리, S_ToonLitTemp=오브젝트 셀 셰이딩)가 각자 다른 몰골 표현을 담당하게 됨 — 향후 정식 에셋(Snapshot Shaders Pro·Flat Kit) 도입 시 이 두 레이어의 파라미터 감각을 레퍼런스로 재사용 가능 |
| 2026-07-24 | **먹 포스트를 "외곽선" 개념에서 "몰골(沒骨) 먹 번짐" 개념으로 전면 재설계(사용자 제공 수묵 담채화 레퍼런스 3장 기반).** 직전에 만든 갈필 외곽선(흔들림+소프트 경계+비백)을 실플레이한 사용자가 레퍼런스 3장(꽃 정물·산수화 2장)을 제시 — 공통 특징 분석: ①윤곽선이 아니라 몰골법(선 없이 먹 농담 자체가 형태) ②하나의 붓질 안에서 진한 먹→마른 비백까지 이어지는 그러데이션 ③결이 살아있는 방향성 비백(붓이 지나간 방향을 따른 무늬, 점무늬 아님) ④거리감도 먹 농도 하나로 표현. 포스트 프로세싱만으론 레퍼런스 수준 완전 재현은 한계가 있음을 먼저 안내(실제 매체의 물리적 확산은 렌더된 이미지 위 후처리로 재현 불가) → 사용자가 "몰골법 근사까지 확장"(더 큰 작업)을 선택. ① 기존 뎁스 소벨 "외곽선"을 선이 아니라 「먹이 뭉칠 씨앗 밀도」로 용도 전환(EdgeStrengthAt 함수로 분리). ② 단일 Hash 점노이즈를 ValueNoise+4옥타브 Fbm으로 교체, 거기에 도메인 워프(좌표 자체를 흐름 벡터로 밀어냄)를 추가해 등방성 점무늬 대신 방향성 있는 붓결 표현(_InkFlowStrength·_InkFlowScale). ③ 경계 씨앗을 흐름 방향으로 여러 반경 샘플링해 max로 모아 확산시킴(_InkSpreadRadius) — 선이 아니라 뭉친 얼룩으로 퍼짐. ④ 씨앗 밀도에 Fbm 블롯치 노이즈를 곱해 뭉친 자리마다 농도가 들쭉날쭉하게(몰골 얼룩 질감). ⑤ 농담 다단계 분리: core(진한 먹, smoothstep 상단)·midtone(중간 번짐, _InkMidbandWidth로 폭 조절)·dryMask(갈필/비백, 결 있는 Fbm 기반이라 이전 점무늬 비백보다 자연스러움) 3겹 합성. ⑥ 프로퍼티 전체 개명(_OutlineXxx → _InkXxx, 개념 전환을 이름에도 반영) — 옛 이름이 M_InkPostTemp.mat에 직렬화 잔존하나 Unity가 새 프로퍼티는 셰이더 기본값으로 자동 채우므로 무해(레거시 정리는 후속 필요시). script-execute로 신규 프로퍼티 7종 전부 머티리얼에 정상 반영 확인, 컴파일 에러·경고 0 | 실플레이로 확산 반경·붓결 방향성·중간톤 밴드폭·비백 세기 손맛 확인(사람) — 전부 [가정] 기본값이라 인스펙터 슬라이더로 바로 조정 가능 | **핵심 교훈**: "외곽선을 부드럽게" 튜닝(1차 갈필 시도)과 "몰골법 근사"(2차, 이번)는 접근 자체가 다름 — 전자는 선 개념을 유지한 채 노이즈로 흐리는 것이고, 후자는 선 개념 자체를 버리고 "먹이 뭉쳐 번진다"는 물리적 은유로 재설계하는 것. 레퍼런스 이미지를 받으면 바로 파라미터 조정에 들어가지 않고 먼저 공통 특징을 말로 분석해 사용자와 맞춘 뒤(이번엔 5개 특징 정리) 구현 범위(부분 개선 vs 전면 재설계)를 물어보는 순서가 유효했음. 포스트 프로세싱의 근본 한계(렌더 후 이미지만 다룸 — 표면 셰이딩 단계의 톤 자체는 재계산 불가)를 먼저 솔직히 알리고 진행 |
| 2026-07-24 | **먹 포스트 외곽선을 수묵 담채 갈필 느낌으로 개조(사용자 요청 — 실플레이 후 룩 피드백).** 지속 시리즈 실플레이 뒤 "테두리가 또렷한 툰셰이더 외곽선처럼 보인다"는 지적으로, 뎁스 소벨 외곽선(S_InkPostTemp.shader)의 계산 방식을 바꿈. 임시 코드 셰이더(에셋 구매 시 교체 예정 스탠드인) 직접 수정 여부를 먼저 확인 후 진행(사용자 승인). ① 샘플 좌표(uv) 자체를 저주파 Hash 노이즈로 살짝 흔들어(_OutlineWobble) 선이 완전한 직선이 아니라 붓 떨림처럼 구불거리게 함. ② smoothstep 문지방 폭을 넓혀(_OutlineSoftness) 칼같은 경계 대신 번진 먹빛 전환으로. ③ 성긴 고주파 노이즈를 선에 곱해(_OutlineDryBrush) 군데군데 먹이 끊기는 비백(飛白) 효과 추가. 3개 신규 Range 프로퍼티를 Properties 블록에 노출(_OutlineSoftness 0~1 기본 0.55·_OutlineWobble 0~0.01 기본 0.0035·_OutlineDryBrush 0~1 기본 0.5), 기존 정적 그레인용 Hash 함수 재사용(신규 노이즈 함수 없음). 컴파일 에러 0, M_InkPostTemp.mat이 새 프로퍼티 기본값을 정상 상속함을 script-execute로 확인 | 실플레이로 갈필 세기 손맛 확인(사람) — 소프트니스·흔들림·비백 3개 슬라이더를 인스펙터에서 직접 튜닝 권장(전부 [가정] 기본값) | 셰이더 코드 직접 수정은 "임시 스탠드인(에셋 교체 전제)"이라 CLAUDE.md의 "셰이더 그래프 직접 저작 금지" 규칙과 상충하지 않음을 사용자에게 먼저 확인받고 진행 — 나중에 Snapshot Shaders Pro 등 정식 에셋으로 교체 시 이 로직(흔들림+소프트 경계+비백 마스크 3단 구성)을 룩 레퍼런스로 재사용 가능 |
| 2026-07-24 | **지속(ㄴ) 오행별 차별화 — 관통·연쇄·단일속박에 이은 4번째이자 마지막 복붙 시리즈 완료.** 설계 논의 중 토(土)의 부가효과가 단일속박("이동 봉쇄")과 겹친다는 사용자 지적으로 "지속 동안 판정 범위가 점점 넓어짐"(주변 적을 새로 끌어들이는 광역 판정)으로 재설계 후 확정. ① FinalModifierConfigSO에 SustainElementConfig[5]+GetSustainConfig 추가(관통·연쇄·단일속박과 동일 배열+조회 패턴). 목=표준 DoT(원형)/화=틱마다 피해 비율 누적 증가(TickFractionGrowthPerTick)/토=주기적 주변 재판정(SpreadRadius·SpreadInterval)/금=틱마다 포이즈 동반 축적(PoiseTickFraction)/수=틱마다 시전자 먹 환급(InkRefundPerTick). ② EnemyStatus.ApplyDot 시그니처 확장(growthPerTick·poiseTickFraction·combatConfig·inkRefundPerTick 선택 인자) — 화는 틱마다 _dotDamagePerTick 자체가 증가, 금은 틱마다 AddPoise 추가 호출, 수는 PlayerBuffLookup.RefundInk 호출. 토는 별도 ApplySustainSpread 메서드 신설 — Update()에서 주기적으로 Physics.OverlapSphere로 반경 내 아직 안 걸린 적을 찾아 같은 지속을 옮겨붙이고(이미 걸린 적은 중첩 방지로 건너뜀), 옮겨붙은 적도 계속 전파(재귀적 확산). ③ PlayerBuffLookup(PlayerCombatStats.cs)에 RefundInk 정적 헬퍼 추가 — 기존 DamageMultiplier 등과 동일한 지연 조회 패턴으로 InkPool을 찾아 Add. ④ SpellProjectile.HitEnemy·SpellAreaBlast.OnTriggerEnter의 Sustain case를 GetSustainConfig 기반으로 갱신(ApplySustainElementEffect 신설). ⑤ CSV 20행(간/건/곤/군·난/넌/논/눈·만/먼/몬/문·산/선/손/순·안/언/온/운) + SD 에셋 Description 20개 일괄 갱신. GetSustainConfig 실측 검증(5속성 전부 의도값 반환 확인, None 폴백도 정상) | 실플레이로 지속 20종 손맛 확인(사람) — 특히 화(틱마다 세짐 체감)·토(주변 확산 범위감)·수(먹 환급 체감). **이제 소환수 10종+관통 20종+연쇄 20종+단일속박 10종+지속 20종, 작도 120자 중 받침 있는 100자 전부(소환 10+관통 20+연쇄 20+속박 10+지속 20+격발 20=100) 오행별 차별화 완료** — 격발(ㅁ)만 "5x5 상합 매트릭스 확정 시 교체" 상태로 의도적 보류 중. 다음은 외부 5인 테스트로 M1 게이트 판정 | 토(土)의 "판정 범위 확장"은 물리적으로 개별 적 상태이상(EnemyStatus)에 "범위" 개념이 없어 구현 방식을 먼저 사용자에게 확인(근처 다른 적도 새로 끌어들이는 광역 판정 vs 시각 표현만 커짐) — 게임플레이에 영향 주는 쪽(권장)으로 확정 후 진행. 관통·연쇄·단일속박 3개 시리즈에서 확립된 "XxxElementConfig[5]+GetXxxConfig" 패턴을 그대로 재사용해 코드 일관성 유지 |
| 2026-07-24 | **연쇄(ㅇ)·단일속박(ㄱ+단일) 오행별 차별화 — 설계 원칙 확립 과정 포함.** 관통에 이어 남은 복붙 시리즈 2개 처리. 설계 확정까지 여러 차례 방향 수정을 거쳤고, 그 과정에서 나온 원칙이 향후 지속(ㄴ) 작업에도 적용돼야 함: **받침 부가효과는 "그 받침 동사에서 자연스럽게 유추되는" 것이어야 한다**(사용자 언급 — 스킬 종류가 많아 전부 외우는 게 불가능하므로). 시행착오: ①연쇄를 관통과 다른 결로 재설계 시도(전파 자체의 성질을 오행별로) → 거절, ②전파 효과가 아닌 다른 효과 제안(먹환급·지형생성 등 오행 속성표 억지 매핑) → "받침 부가효과는 동사에서 유추돼야" 원칙으로 거절, ③올바른 해석 확인: "연쇄=퍼진다"이므로 부가효과 자체가 연쇄를 타고 함께 퍼지는 방향(권장)으로 최종 확정. 단일속박도 "감속"이 아니라 "행동을 방해한다"는 상위 컨셉으로 확장(사용자 결정) — 화·토는 텔레그래프(예비 동작) 배율, 수는 조준 오차라는 새 개념이 필요해 EnemyAI에 새 훅 2종 추가(회피 개념 자체가 적 AI에 없어 "회피 봉쇄"는 제외됨을 확인 후 진행). ① EnemyStatus에 TelegraphMultiplier·AimJitterDegrees 필드 추가, ApplyBind 시그니처 확장. EnemyAI.TelegraphSeconds·Attack()의 조준 계산에 반영. ② FinalModifierConfigSO에 BindElementConfig[5]·ChainElementConfig[5] 추가(관통과 동일 패턴, GetBindConfig/GetChainConfig). 목=표준속박/화·토=속박+공격느려짐/금=속박+지속피해/수=속박+조준흔들림. 연쇄는 목=속박전염/화=화상전염(점프마다 길어짐)/토=그로기누적(점프마다 커짐)/금=부가효과없음(전파력 자체 특화)/수=감속 고르게 유지 — SustainSecondsGrowthPerJump·ExtraPoiseGrowthPerJump로 "누적" 표현. ③ SpellProjectile.HitEnemy의 Bind/Chain case 갱신(ApplyChainElementEffect 신설, jumpIndex=ChainMaxJumps-_chainJumpsLeft로 점프 순번 추적). ④ **부수 발견·수정**: SpellAreaBlast(광역 파동)에 애초부터 관통·연쇄 처리가 아예 없었음을 발견 — 광역×관통/연쇄 조합 20자(곳/굿/공/궁 등)가 그동안 그냥 1회 타격으로 끝났을 것. 이번에 같이 고침(광역판은 대상 수 제한 대신 부가효과만, 점프 인덱스는 _alreadyHit.Count로 대체). ⑤ CSV 30행(연쇄 20+단일속박 10) + SD 에셋 Description 갱신(30/30 검증), FinalModifierConfigSO 실측 검증(GetBindConfig·GetChainConfig 5속성 전부 의도값 반환 확인) | 실플레이로 연쇄 20종(화상 누적·그로기 누적 체감)+단일속박 10종(화/토 공격 지연·수 조준 흔들림 체감) 검증(사람). 지속(ㄴ) 시리즈만 아직 복붙 상태로 남음 | **핵심 교훈**: 오행 세계관 자료를 받아도 곧바로 반영하지 말고 "이미 구현된 것과 겹치는지" + "받침 동사에서 자연스럽게 유추되는 효과인지" 두 필터를 거쳐야 함. AskUserQuestion 인코딩 이슈: 한글 문자열에 유니코드 이스케이프(특히 스마트따옴표 “/”)를 섞으면 JSON 파싱이 반복 실패 — 일반 ASCII 따옴표만 쓰거나 이스케이프를 완전히 배제할 것 |
| 2026-07-24 | **작도 120자 감사 — 받침 시리즈 "복붙" 점검 + ㅅ(관통) 오행별 차별화.** 사용자 요청으로 CSV 120행 전수 확인: 소환수(ㄱ+영역, 방금 오행화 완료)를 빼면 관통(ㅅ)·연쇄(ㅇ)·단일속박(ㄱ+단일)·지속(ㄴ) 4개 받침 시리즈가 FinalModifierConfigSO 단일 값(PierceMaxTargets=3, ChainMaxJumps=3 등)을 오행 구분 없이 공유 — 코드 레벨에서 "복붙" 확인. 격발(ㅁ)은 PROGRESS에 이미 "5x5 상합 매트릭스 확정 시 교체 예정"이라 기록돼 있어 제외. 사용자 결정: 이번엔 관통(ㅅ)만 진행, 연쇄·단일속박·지속은 다음 기회. ① 사용자가 오행 상생상극 원전 자료 제공 → 오행 발췌·설계 반영 여부 확인 절차 거침(개별 능력치 안 그대로 유지, 상생상극 순환은 이미 ElementRelationTableSO가 담당하므로 관통엔 추가 반영 안 함 — 사용자 확정). ② FinalModifierConfigSO에 PierceElementConfig 구조체 + PierceByElement[5] 배열 추가(GetPierceConfig로 오행별 조회, 값 0=효과없음 컨벤션). 목=관통수3·속박(0.4배·1.2s) / 화=관통수2·짧은 지속화상(20%) / 토=관통수2·속도0.8배·포이즈추가축적(20%) / 금=관통수5·속도1.3배·부가효과 없음(순수 관통력 특화) / 수=관통수3·끌어당김(2.5). ③ SpellProjectile.Init에서 오행별 MaxTargets·SpeedMultiplier 적용, HitEnemy의 switch에 Pierce case 신설(ApplyPierceElementEffect) — 목/화/토는 기존 EnemyStatus 로직(ApplyBind/ApplyDot/AddPoise) 재사용, 수는 신규 PullPreviousPierceTargets(관통한 대상들을 서로 끌어당김, 방향 찢김 방지로 첫 매칭 대상 하나에만 적용). ④ CSV 20행(갓/것/곳/굿·낫/넛/놋/눗·맛/멋/못/뭇·삿/섯/솟/숫·앗/엇/옷/웃) + 대응 SD 에셋 Description 전부 갱신. GetPierceConfig 실측 검증(목/화/토/금/수 5속성 수치 전부 의도대로 반환 확인) | 실플레이로 관통 20종 손맛 확인(사람) — 특히 수(끌어당김 세기)·토(느린 대신 그로기 축적 체감) | AI가 사용자 제공 오행 원전 자료를 받았을 때 그대로 설계에 밀어넣지 않고 "이미 구현된 것과 겹치는지" 먼저 확인 후 반영 여부를 물어본 것이 유효했음(상생상극은 이미 ElementRelationTableSO가 전담 — 중복 구현 방지). 연쇄·단일속박·지속 3개 시리즈는 여전히 복붙 상태로 남아있음(다음 세션 후보) |
| 2026-07-24 | **소환수 5속성 오행별 동사 확장(사용자 결정 — "속성마다 독특한 행동").** 목만 완성했던 소환수를 5속성 전부로 확장하되, 전투코어루프 §9 "속성=공격 동사" 원칙(EnemyArchetype 선례)을 그대로 계승해 공격형 5종을 서로 다른 동사로 분리. ① SummonDefinitionSO에 AttackVerb enum(ProjectileVolley/FireZone/SlowSlam/SniperPierce/ChainPulse) + 동사별 전용 파라미터(Odin ShowIf로 역할·동사에 맞는 필드만 노출) 추가. ② SummonAttack을 동사 분기 구조로 재작성: 목=투사체 연사(기존)·화=제자리 화염 지속 피해장(주기 판정)·토=예비 동작(텔레그래프) 후 굵고 느린 광역 고피해 타격·금=긴 사거리 저격 관통탄(SpellProjectile Pierce 재사용)·수=주기적 인접 전체 동시 연쇄 파동. ③ 버프형도 속성별 차별화 확정(사용자 확인): 화=공격력 증가/토=방어력 증가/금=포이즈 축적 배율 증가(그로기 유도 쉬움, "누구의 포이즈?"를 확인해 적의 포이즈로 확정)/수=이동속도 증가, 목=회복(기존). ④ PlayerCombatStats·PlayerBuffLookup에 PoiseDamageMultiplier 추가, MeleeAttack·SpellProjectile·SpellAreaBlast의 AddPoise 호출에 배율 적용(SpellAreaBlast는 공격력 버프도 누락돼 있어 이번에 같이 연결). ⑤ 화/토/금/수 SM_*_Attacker·SM_*_Buffer 8개 에셋 생성, SpellCaster._summonDefinitions를 10건 전체 배선. ⑥ 「녹/눅/목/묵/속/숙/옥/욱」 8개 SD 에셋 Modifier를 Bind→Summon, Description 재작성 + CSV 원장 동기화(목 2개 포함 총 10글자 Modifier=Summon 확인). 컴파일·전체 10건 데이터 배선 script-execute로 검증 완료 | 실플레이로 5속성 10종 소환수 전부 체감 확인(사람) — 특히 토(예비 동작 타이밍)·수(연쇄 파동 범위)·금(저격 사거리) 손맛 튜닝 | 「목」(ㅁ토+ㅗ+ㄱ)과 「국·묵·숙·욱」처럼 글자 자체가 속성마다 다른 초성이라 헷갈리기 쉬움 — 토(土) 소환 관련 글자가 하필 "목"인 점 주의(목=Wood 속성 대표 글자와 동음이의). SummonAttack.FireSniper는 소환수 생애당 FinalModifierConfigSO 인스턴스 1개만 캐싱(발사마다 새로 안 만듦, OnDestroy에서 정리) |
| 2026-07-23 | **소환수 시스템 신설 — ㅗ/ㅜ+ㄱ받침 전용 거동(사용자 결정, 작도 120자 완성 프로젝트 착수).** 규칙 확인: 종성=속성별 1개 거동 원칙은 유지하되, ㄱ(Bind)에 Scope(영역) 축을 얹어 예외를 추가하는 방식으로 CLAUDE.md와 충돌 없이 설계(단일 ㅏ/ㅓ+ㄱ은 기존 속박 그대로). 범위: 5속성 확장 가능 공통 구조, 이번 세션은 목(木) 1속성만 완성. ① FinalModifier enum에 Summon 추가. ② PlayerCombatStats(신규) — 데미지·이속·방어 배율 허브, 여러 버프 소스가 겹쳐도 최댓값/최솟값만 반영(곱 누적 방지), source 키로 버프 걸고 뗌. PlayerBuffLookup 정적 클래스로 런타임 생성 오브젝트(투사체 등)도 씬 배선 없이 조회. MeleeAttack·SpellProjectile·PlayerHealth(방어 배율+Heal 메서드 신설) 연결. ③ SummonDefinitionSO(신규, Data) — Role(Attacker/Buffer) enum, 오행 확장 가능 구조, Odin ShowIf로 역할별 필드 토글. ④ SummonAttack(설치형, FindObjectsByType로 범위 내 최근접 적 자동 타겟팅 → SpellProjectile 재사용 발사) / SummonBuff(플레이어 추종, 범위 내일 때만 PlayerCombatStats.ApplyBuff, 이탈 시 RemoveBuff, 목=회복 HealPerSecond) 신규. ⑤ SpellCaster.Cast()에 Modifier==Summon 최우선 분기(Category 무관) → CastSummon, Category로 Attacker/Buffer SO 구분(같은 속성도 곡=Attack/국=DefenseControl로 다른 SO 매칭). ⑥ 「곡」「국」 SD 에셋 Modifier를 Bind→Summon 변경 + Description 재작성, CSV 원장 동기화, SM_Wood_Attacker/SM_Wood_Buffer 에셋 생성, SpellCaster._summonDefinitions·PlayerCapsule에 PlayerCombatStats 씬 배선. 개수 제한 없음(먹 비용으로만 억제 — 사용자 결정). 컴파일·데이터 배선·버프 산술(2버프 동시 적용→최댓값, 제거→1.0 복귀) 전부 script-execute로 검증 완료 | 실플레이로 「곡」「국」 실제 소환·전투 검증(사람) — 공격 소환수 타겟팅·버프 적용/해제, 성공 시 화/토/금/수 4속성으로 확장 | 소환수는 적 공격의 타겟이 되지 않는 무적 존재로 단순화(적 AI가 EnemyHealth만 타겟팅하는 기존 구조라 자연히 그리됨 — 필요시 후속으로 피격 가능하게 확장). 이동속도 버프는 StarterAssets(asmdef 미참조 서드파티)에 직접 곱하지 못해 배율 값만 노출 — 실제 반영은 별도 글루 필요(M1 자체 컨트롤러 도입 시 정리) |
| 2026-07-23 | **목(木) 폭발을 나뭇가지 전용 연출→공통 경로로 되돌림(사용자 결정 — 방향 전환) + 병합 후 텍스처 깨짐 진단.** ① 브랜치 전환 중 발생한 "텍스처 깨짐"은 gitignore 대상 대용량 구매 에셋(`KoreanTraditionalPattern_Effect`, 8.3GB)이 로컬에 일부만 있던 상태였음을 규명 — Git 히스토리·머티리얼 GUID는 정상이었고, **에셋 재다운로드 + AssetDatabase 강제 리프레시**로 완전 해결(5속성 투사체·2종 폭발 프리팹 전부 활성 렌더러 NULL 슬롯 0개로 재검증). 병합 되돌리기는 불필요·오히려 커밋된 스크립트 작업까지 유실 위험이라 하지 않음. ② 사용자가 목 전용 나뭇가지 폭발(LineRenderer BranchGrowth, 지난 세션 내내 튜닝)을 폐기하기로 결정 — 이유: 다른 4속성과 같은 Fly Explosion 문양 폭발로 통일하고 색만 갈색+녹색 혼합으로 구분하는 편이 낫다고 판단. SpellProjectile.PlayImpact의 목 특수분기 삭제(공통 SpawnPatternExplosion+SpawnGroundStamp 경로로 흡수), SpellVisuals.cs에서 SpawnBranchBurst·BranchGrowth·ConfigureInkBleed·BranchLineMaterial·미사용 GlowMaterial 전부 제거(_glowTex는 InkBlobMaterial이 재사용하므로 존치). ③ 색 변경: ElementPalette.asset Wood (0.2,0.85,0.45)→(0.42,0.58,0.24), SpellCaster.ElementTint 코드 폴백값도 동일하게 맞춤 — 실측 검증 완료 | 실플레이로 갈색+녹색 혼합 톤 확인(사람), 목 폭발이 다른 속성과 동일 룩인지 최종 체감 | **교훈**: gitignore된 대용량 구매 에셋(가짜 "텍스처 깨짐"의 흔한 원인)은 브랜치 문제가 아니라 로컬 파일 상태 문제 — git status에 안 잡히면 재다운로드/재임포트부터 의심. 나뭇가지 연출은 여러 세션에 걸쳐 튜닝했지만 최종적으로 사용자가 통일된 룩을 선호해 폐기 — 사람 최종 판단이 이전 반복 작업량보다 우선 |
| 2026-07-23 | **작도 붓 효과음(ElevenLabs) + PR 생성.** ① feature/dev→main PR #2 생성(39커밋, 진 이펙트 재구축 대표). ② ElevenLabs sound-generation API로 붓 소리 4종 생성(HTTP 200 — "TTS 전용 제한 키"여도 사운드 생성 가능 확인): 젖은 붓 루프·마른 붓 루프(각 4s)·짧은 스위시 2종. 원본 Tools/generated/audio 보존, 선별본 Assets/_Project/Audio/SFX(SFX_ 접두 [가정]). ③ DrawingInputController에 IsStroking 프로퍼티(마우스 다운=true, 업·ClearDrawing=false — 모드 이탈 안전). ④ BrushStrokeAudio 신설(Spellcraft): 획 긋는 동안 루프 재생, **커서 속도 비례 음량·피치**(빠른 획=거세게·멈춤=잦아듦), 매 획 다른 재생 지점(반복감 감소), 기필 원샷 슬롯(기본 비움), 작도 감속 대비 unscaled 시간 기준. InGame 씬 DrawingInput에 부착·SFX_BrushLoopWet 배선·저장 | 실플레이로 소리 톤·음량 커브 판정(사람) — 마른 붓 루프/스위시 교체 시험, 먹 잔량 연동(마른 붓=갈필 소리)은 후속 후보 | 소리 판정은 사람(룩과 동일 원칙). 브랜치 feature/sound, 미커밋 — 청감 확인 후 커밋. ElevenLabs 키 권한 메모 갱신: sound-generation 사용 가능 |
| 2026-07-22 | **목 폭발 원형진 삭제 + 수묵 담채화(붓 텍스처+담채+먹번짐).** ① 원형진(SpawnGroundStamp) 목 폭발에서 제거(사용자 결정) — 가지만. ② 수묵 담채(사용자 선택=붓텍스처+담채·번짐): 가지 LineRenderer에 **InkStroke 절차 붓 아틀라스(6행 비백·농담)** 입힘 — Combat→Spellcraft 참조로 InkStroke.CreateBrushAtlas/ProceduralAtlasRows 재활용, textureMode=Stretch(기필→수필), 가지마다 material 인스턴스+행 오프셋 무작위(상단 절반=촉촉~중간)로 붓결 변주. ③ 색을 담채로: bark 0.5/0.31/0.14→0.4/0.29/0.19(담묵 세피아), sprout 채도 낮춤(0.5/0.68/0.4 이끼). ④ 발광 레이어를 **먹 번짐(ConfigureInkBleed)**으로 교체 — 가산 초록 글로우 대신 알파블렌드 담묵 얼룩이 느리게 번져 스밈(InkBlobMaterial 재활용, 알파 0.35). screenshot-isolated 검증(#E8E0D0 종이톤 배경): 선명 벡터선→먹빛 담채 붓획으로 톤 전환 확인 | 실플레이 최종 — 붓 비백 체감·먹번짐 세기·담채 톤 판단(사람) | 붓 아틀라스는 mainTextureScale.y=1/행수 + offset.y=행/행수로 한 행만 샘플. 비백은 작은 스트림선 크기에선 미묘(끝 테이퍼로 나타남). GlowMaterial은 이제 미사용(무해, MakeSoftGlowTexture는 InkBlob이 씀) |
| 2026-07-22 | **목 폭발 LineRenderer 크기·투명·지속 추가 축소(사용자 피드백 2·3차).** 2차: 알파 0.82→0.5, 유지 2.4→0.8s·페이드 0.8→0.27s·파괴 4.5→1.5s(지속 1/3). 3차: 크기 절반(reach 1.4→0.7배, baseWidth 0.115→0.0575배), 알파 0.5→0.3, 지속 추가 절반(유지 0.4s·페이드 0.135s·파괴 0.85s) — 지속 짧아진 만큼 성장시간도 단축(주가지 0.28~0.42→0.16~0.24s, 잔가지 0.22→0.13s)해 가지 잘림 방지. screenshot-isolated 재검증: 절반크기·알파0.3에서도 갈색 가지 다발 또렷(파티클과 달리 LineRenderer는 저알파에도 안 사라짐) | 실플레이 최종 | 알파·크기·지속은 상수·배율이라 값 그대로 반영. 지속 줄일 땐 성장시간도 같이 줄여야 가지가 자라다 페이드에 잘리지 않음 |
| 2026-07-22 | **목 폭발 LineRenderer 가지 톤 조정(사용자 피드백).** 사용자 "느낌 아주 좋다" 후 미세조정 4건: ① 갈색빛 강조(bark 0.42/0.26/0.12→0.5/0.31/0.14 따뜻한 갈색, 끝 초록 lerp 0.6→0.35로 초록 비중↓) ② 살짝 투명(가지 알파 1.0→0.82) ③ 위 편향→사방 구형(Random.onUnitSphere + y 0.15 약한 위 바이어스만, 바닥관통 방지) ④ 크기 축소(reach 1.7→1.4배, baseWidth 0.14→0.115배). screenshot-isolated 재검증: 갈색 주도+끝 살짝 초록·반투명·전방위·컴팩트 확인 | 실플레이 최종 | 목업 렌더로 색·방향·크기·투명도 확정 후 실제 컴포넌트 반영(동일 파라미터) |
| 2026-07-22 | **목 폭발 근본 재설계 — 파티클 가지→LineRenderer 가지(스크린샷 검증) + 화염퍼프 정체 규명·제거 + 수묵 사망연출 + 발광 축소.** ① 화염퍼프 정체: 스크린샷 hierarchy에 vfx_Explosion_02(Clone) 발견 → GUID(83ad694f…) 추적 → EnemyHealth._deathVfxPrefab(적 사망 시 재생, 씬 12기 전부 배선)임을 규명. 코드가 부르는 게 아니라 적 처치 순간 재생돼 "명중과 무관해 보였음". ② 사망연출 수묵 교체(사용자 결정): SpawnInkDeathBurst(먹방울 튀김+먹구름 피어오름, 알파블렌드 먹빛) 신설, EnemyHealth.Die가 호출, 씬 12기 _deathVfxPrefab=null로 비움+저장. ③ **가지 불가시 근본 해결**: screenshot-isolated로 실제 렌더 확인 → 파티클 스트림(Stretch+스파이크텍스처)은 밝은 배경서 거의 안 보임을 눈으로 확인(입자 18개·알파1·크기0.9인데도 화면엔 흐린 점 하나). 원인=stretch 얇은 슬리버+반투명 텍스처. **해결: LineRenderer 실선 가지로 전면 교체**(BranchGrowth 컴포넌트) — 명중점에서 12 주가지가 자라나고(SmoothStep reveal 0.35s) 끝에서 잔가지 갈라짐, 굵기 taper·갈색→초록 그라디언트·LineAlignment.View. 동일 배경 재렌더: 갈색밑동+초록끝 가지 다발이 또렷이 보임(검증 완료). ④ 발광 블룸 축소(startSize 0.7~1.3→0.28~0.55배). ⑤ 명중 시 KillFlightVisual로 비행본체 화염 클리어(전 세션). ⑥ 벽 미폭발=가지 불가시와 동일 원인 추정(벽 콜라이더는 정상 non-trigger 확인) | 실플레이로 성장 애니·잔가지·페이드·수묵 사망·발광 크기 최종 확인(사람) | **핵심 교훈: 파티클 "안 보임"은 값 튜닝(알파·크기·색)으로 3회 실패 → screenshot-isolated로 실제 렌더를 눈으로 봐야 근본원인(렌더모드) 잡힘. Stretch 파티클 가지는 밝은 배경서 부적합, 확실한 형상엔 LineRenderer가 정답.** LineRenderer 성장은 Update 구동이라 편집모드 스샷엔 안 보임 → 완전성장 목업을 직접 그려 가시성 검증 | 
| 2026-07-22 | **목 폭발 비주얼 킥 — 화염퍼프 제거 + 가지 증량 + 발광 레이어.** ① 명중 순간 화염/불빛 퍼프 제거: 사용자 지적한 "기존 폭발"은 비행본체(Fly 화염 트레일 FxTrail_05/Flame)가 명중 시 남기는 잔류 불꽃 → SpellProjectile.KillFlightVisual()로 명중 시 PatternProjectile 파티클 Clear(검증: 55개 시스템 클리어). ② 가지 증량: 주가지 burst 8→15(입자 19), 잔가지 4→6(입자 98). ③ 발광 레이어(ConfigureGlow): 명중 순간 초록 발광(0.55/1/0.4) 중심 블룸+빛무리 14~18개가 팍 부풀었다 사라짐, 가산혼합(SrcAlpha/One)·소프트 원형 글로우 텍스처(MakeSoftGlowTexture)·sortingOrder -1(가지 뒤)·수명 0.35~0.7s 짧게(배경 안 날리게). 검증: 3레이어 동시 방출 확인(글로우18/주가지19/잔가지98) | 실플레이로 킥 체감·발광 세기 조정(사람) — 너무 밝으면 알파/개수↓, 부족하면↑ | 발광은 가산혼합이라 밝은 배경서 흰색으로 날 수 있어 알파 0.7~0.9·단명으로 억제. Clear(true)는 자식 포함이라 루프는 중복이나 무해 |
| 2026-07-22 | **목 폭발 가시성 2차 — 색 제곱 버그 수정 + 굵기·불투명도·지속 확대.** 사용자 "육안으로 안 보임" 반복 → 근본원인 발견: ConfigureBranch가 startColor=bark로 두고 colorOverLifetime도 bark라 **두 색이 곱해져(bark²) 렌더 색이 ~2배 어두워 흐려짐**. startColor=흰색으로 바꿔 gradient 색이 온전히 발현되게 수정(검증: 렌더 입자색 RGBA(0.50,0.32,0.14,1.0) — 의도한 bark 그대로·알파 1.0). 추가: startSize 0.16~0.26배(약 2배 굵게, 평균 0.92m)·sizeCurve 밑동부터 도톰(0.6→1.0→0.8→끝만 뾰족)·lengthScale 5→3.8·velocityScale 0.34→0.28(덜 얇게)·알파 0.95까지 100% 유지·bark 더 짙게(0.5/0.32/0.14)·sprout 짙게(0.4/0.95/0.28)·스파이크 텍스처 심 꽉 채움(가장자리 15%만 페더). 지속시간 총 5s·주가지 수명 1.5~2.1s·잔가지 0.9~1.5s. 바닥진 alpha 0.55 반투명 | 실플레이로 이제 잘 보이는지 확인(사람) — 부족하면 reach/개수/발광 레이어 추가 검토 | 교훈: 파티클 startColor와 colorOverLifetime은 곱해진다 — 둘 다 색을 넣으면 제곱으로 어두워짐. 색은 한쪽(gradient)에만, startColor=흰색이 정석. 알파만 올려선 "흐림"이 안 잡힘 |
| 2026-07-22 | **"가"(목) 투사체 한 갈래화 + 목 폭발 폴리시(2단 성장) + 폭발 바닥진 빌보드.** ① 진단: 편집모드 결정론 시뮬(Simulate)로 SpawnBranchBurst가 입자 17개 정상 방출·렌더 확인 → 폭발 코드는 정상, 원인은 "명중 미발생"(광장 허공 소멸). 임시 [BB진단] 로그 심어 사용자 실플레이로 명중 시 폭발 뜸 확인(단지 너무 흐렸음). ② **투사체 완전 한 갈래**: KeepSingleTrailStream(FxTrail_05만 남기고 01/03/07/09 비활성)에 더해, 남긴 스트림 안의 보조 리본 Fx_Light_Trail(작은 곁줄기)도 비활성 → 활성 FxTrail 1개·Flame만 ON 검증. ③ **폭발 폴리시(2단 성장 강조)**: bark 0.62/0.42/0.22·sprout 0.5/1/0.4로 진하게, reach 1.8→2.6배·굵기↑, 주가지 감속 강화(뻗다 멈추는 가지)·수명 0.75~1.1s, Sub Emitter로 주가지 Death 시 잔가지 방출(시차) — 검증: 주가지 8→잔가지 49(2단 정상). ④ **폭발 바닥진 카메라 빌보드**: SpawnGroundStamp에 faceCamera 옵션 + PatternFacingBillboard(원반 +Y 노멀이 카메라 향함, FromToRotation), 목 폭발에서만 세워서 각인. ⑤ course 무한반복=게임버그 아님(로그 클린) — 이전 세션 원격 캡처 루프 추정, 이번엔 실플레이 검증 위주 | 실플레이로 폭발 손맛·진함·크기·나무다움 최종 판단(사람), 바닥진 빌보드 각도 어색함 여부, 나머지 4속성 폭발 다양화 | 진단 교훈: 파티클 "안 보임" 신고 시 Simulate+particleCount로 먼저 방출 여부부터 가르면 명중 문제/렌더 문제/구성 문제를 즉시 분리 가능. 편집모드 테스트에서 SpawnBranchBurst 내부 Object.Destroy(go,t)는 benign 에러(플레이모드 정상). renderer.material도 편집모드에서만 leak 경고 |
| 2026-07-22 | **투사체 파티클 감축 + 목 나뭇가지 폭발(임시).** ① 사용자 피드백 "가 발사 시 Flame·Thunder 등 여러 갈래가 같이 발사됨" → 5속성 Element_Projectile 프리팹에서 중복 FxTrail 짝수갈래 + Impact 계열(명중용, 비행 불필요) 제거해 파티클 절반 감축(Wood 102→55, Fire 71→39 등). ② 폭발을 속성별 분기: SpellProjectile.PlayImpact에서 목(木)이면 화염 대신 SpawnBranchBurst. ③ **[임시] 목 나뭇가지 폭발**: 갈색 주가지가 방사형 stretch 파티클로 뻗고, Sub Emitter(Death 트리거)로 잔가지 갈라짐, colorOverLifetime로 끝갈래 초록(새순), 코드 생성 뾰족 텍스처(MakeSpikeTexture). 임시 작업 정리 점검 완료(무적 OFF·속도 25·PROGRESS 추적 양호) | 실플레이로 목 폭발 손맛 조정(크기·잔가지·초록 타이밍), 나머지 4속성 폭발 다양화, 정식 나뭇가지 에셋/생성 | 목 폭발은 임시 코드 파티클 — 정식은 에셋/생성 API로 교체 예정(코드 [임시] 명시). 원격 격리렌더로 파티클 애니 순간 포착 불안정 → 실플레이 검증 필요. tint 무시하고 갈색→초록 고정(목 정체성) |
| 2026-07-22 | **진 이펙트 전면 재구축 — Fly 에디터 사전 분해.** 워크플로우로 에셋 재분석(애니메이션 클립 파싱): Fly는 Animator 타임라인이 Projectile(1.33s ON, x -4.8→+4.8 비행)·Explosion(2.33s ON) m_IsActive를 순차 제어하는 2.3초 발사→비행→폭발 컷신임을 확정. 런타임 뜯기가 모든 버그의 근원(파티클 시차 무시→폭죽화). 해결: **에디터에서 오행 5속성 Fly를 Element_Projectile(비행)/Element_Explosion(폭발) 프리팹으로 사전 분해**(Art/Effects/SplitFly, Animator 제거·그룹 활성화 굽음). SpellVisuals에서 KeepOnlyGroup 로직 전부 제거→분해본 그대로 부착. SpellPatternSetSO에 Explosion 슬롯 추가. 명중 시 Explosion+Bottom 전통문양 함께 각인(SpawnGroundStamp). 검증(pause+Step): 비행 중 폭발 0·비행 궤적 정상·명중 시 화염폭발+연꽃문양 각인 확인. 색 채도 0.35→0.7(테스트) | 실플레이 최종·톤 결정·파동/막/돔에도 분해 방식 확대 여부 | 분해 프리팹은 gitignore된 원본 에셋의 파티클·머티리얼 GUID 참조 → 원본 재임포트 시 재분해 필요. Fly 클립 90종 곡선 동일(변주는 머티리얼만) |
| 2026-07-22 | **투사체 문양 안 보임 수정.** 원인: Fly 프리팹 루트 Animator가 발사→비행→폭발 타임라인으로 Projectile 그룹을 특정 시점에 SetActive(false) → Charge/Explosion 지워도 Animator가 남아 Projectile을 꺼버림(살아있는 입자 0). 해결: KeepOnlyGroup에서 Animator 제거 + 남긴 그룹·하위 파티클 강제 SetActive(true). 검증(pause+Step): 살아있는 입자 0→2254, 카메라 렌더로 소용돌이 문양 투사체 가시화 확인 | 실플레이 최종(명중 폭발 손맛·톤) | Fly 프리팹은 Animator 타임라인 구동 완결 이펙트 — 부분 추출 시 Animator 제거 필수 |
| 2026-07-22 | **투사체 폭발 분리 — 비행/명중 이벤트 분리.** 원인: Fly 프리팹은 발사→비행→폭발이 3개 최상위 그룹(Charge -4.8 / Projectile -4.8 / Explosion +4.8)으로 좌우 배치된 완결 이펙트인데, 오프셋 자식을 로컬 0으로 모으면서 Explosion(rate=10, 5초)까지 투사체 위치에서 방출돼 "투사체 왼쪽에서 폭발" 현상. 해결: SpellVisuals.KeepOnlyGroup으로 비행 시 Projectile 그룹만, SpawnPatternExplosion으로 명중·벽충돌 시 Explosion 그룹만 그 지점 재생. SpellProjectile.SetExplosionPattern/PlayImpact 추가(수명만료=허공은 폭발 없음). 검증(pause+Step 프레임 강제): 비행 정상(life 5.0→3.8, 7m 전진)·비행 중 폭발오브젝트 0·SpawnPatternExplosion 파티클 773 방출 확인 | 실플레이 최종 확인(명중 폭발 손맛), 투사체 톤 | 원격 검증 핵심 교훈: 에디터 플레이 중 Game뷰 비활성이면 Update 정지 → 투사체가 안 움직이는 것처럼 보임. EditorApplication.isPaused=true + Step() 반복으로 프레임 강제 진행해야 실제 비행 검증 가능. Room_Minions는 트리거라 투사체 통과(폭발 안 함) |
| 2026-07-22 | **문양 이펙트 폴리시 2차(5건).** ① 진 발동 텍스트(AttachLetter) 3곳 제거 ② 투사체 발사 위치: Fly 프리팹이 발사→비행→폭발 시퀀스를 로컬 공간에 펼쳐둔 것(Projectile 자식 -4.8 등) → 오프셋 큰 파티클 자식을 로컬 0으로 정렬, SpellProjectile에 스폰 후 충돌 유예(_armDelay 0.06s, 붓끝·근처 벽 오탐 방지) ③ 전방막(ㅓ): 앞쪽에 여러 층(_shieldWallLayers 4) 쌓아 벽처럼 완성감, DetachAndFade 헬퍼 ④ 바닥: House Of Changwon T_Floor_Brick(전돌 포장)으로 M_Ground_Brown 교체(URP Lit, 타일링 8x3) ⑤ 방어막 지속 8→4초(SD 50건) | 2번 투사체 실플레이 확인(격리캡처로 미검증), 바닥 타일링 세밀 튜닝, 톤 최종 판단 | SmartMaterials Vol.4는 단청 목재(나무결) 전용이라 흙길 없음 → KCISA House Of Changwon 바닥재 사용. SD_다 없음(화 투사체는 SD_나=ㄴ화+ㅏ). 2번은 원격 격리 렌더로 문양이 안 잡혀 실플레이 확인 필요 |
| 2026-07-22 | **문양 진 이펙트 폴리시 4건(수묵담채 톤).** ① 발사 위치: 카메라 원점→붓끝 muzzle 오프셋(_muzzleLocalOffset) ② 끊김 방지: PatternFade 컴포넌트(등장 스무스+방출 끊어 잔류 자연소멸), 바닥진·방어막 문양을 판정 부모에서 떼어 독립 소멸, 방어막 지속 8초(ShieldEffect 50건) ③ 전방막(ㅓ): 세운 원형진이 방사선 하늘로 뻗어 어색→플레이어 앞 바닥진 방식(반구형)으로, 위치 낮춤 ④ 톤다운: ApplyTint를 파티클 startColor뿐 아니라 렌더러 머티리얼 _Color/_TintColor/_BaseColor까지 확장(흰 문양선 Pattern/LineLight 억제), InkSaturation 0.35·InkValueScale 0.5로 수묵담채. 검증: 목 전방막 담채 청록 확인 | 1인칭 실플레이로 손맛·시야·톤 최종 판단(사람), 화/토/금 확인, 진 형태 가독성 | 흰 선 정체=커스텀 ShaderGraph(AdditiveBlend/AlphaBlend)의 _Color 흰색+HDR(2,2,2). 가산혼합은 밝은 배경서 흰색으로 날아가 명도 특히 낮게. 문양 startColor는 런타임 실측으로 틴트 적용 확인(격리캡처는 Simulate 색과 다를 수 있음) |
| 2026-07-21 | **공격 이펙트 문양 교체 — Korean Traditional Pattern Effect 에셋 적용.** 진(陣) 발동 시 반투명 프리미티브를 전통 문양 진(연꽃·당초·완자·꽃무늬)으로 완전 교체. ① SpellPatternSetSO(오행→Bottom 바닥진/Fly 발사체 매핑, 데이터 주도) ② SpellVisuals에 AttachGroundPattern/AttachProjectilePattern/HideRenderer + ApplyTint(오방색 강제) 추가 — 판정 프리미티브는 렌더러만 끄고 콜라이더·스크립트 유지, 게임플레이 무변경 ③ SpellCaster 4발동(투사체·파동·전방막·돔)에서 문양 스폰+ElementTint(오방색). 검증: 목=청록·수=청 확인(목11→03 등 형태 우선 재배정, 색은 틴트가 강제) | 화/토/금 인게임 최종 확인, 진 크기·높이 미세 튜닝, 뷰모델과 합쳐 손맛 | 에셋 startColor 실측: 청록·주황 위주라 오방색과 불일치 → 명도 유지 색상 교체 틴트로 해결. 정투영 격자 색 판독은 조명 탓 부정확 → 파티클 startColor 직접 추출이 정확. RP 머티리얼 컨버터 배치모드 버그(Base2DMaterialUpgrader) → 수동 변환 |
| 2026-07-21 | **UI 1차 + 사냥 방·배달 시스템 + 한옥 마을 비주얼 테스트.** ① 임시 UI: Recraft 스프라이트(한지 패널·부적·통보·먹 번짐·낙관, 먹획 게이지는 기존 아틀라스 재사용) + Wolin 폰트, 메인 메뉴 씬(시작/이어하기(비활성)/설정 4탭(그래픽 실동작·음향 마스터 실동작·조작 저장·접근성 흔들림 실연동)/크레딧/종료, Boot→MainMenu→InGame), 인게임 HUD(HP 주묵·먹 흑묵 먹획 게이지, 부적 슬롯, 화물 쪽지, 통보 팽창 표시) — 기존 OnGUI 정리 ② 사냥 방(榜): BountySO+게시판, 적 처치 SO 채널(CH_EnemyKilled, 12기 배선) ③ 배달: DeliveryMissionSO·PlayerCargo(피격 시 상함·사망 시 실패)·수령/납품처 — 검증 32문 정확 ④ kcisa House Of Changwon: Standard→URP 수동 일괄 변환(70건, RP컨버터 버그 우회), 남쪽 개활지(-36,0,-44) 한옥 마을 배치(안채·사랑채·창원마루·팔각정·방앗간·정문·담장+소품 30여 점), 부활·게시판·화물함 마을 이사 | UI 손맛 피드백, 다음 개체 컨셉아트, 해태 보스전 | Recraft 교훈: 평면 텍스처는 "프레임 가득·수직 부감·무그림자" 명시(아니면 원근 사진). 상호작용 프롬프트는 아직 OnGUI(디제틱 후속). 광장 단상은 구 프로토 지오메트리로 잔존 |
| 2026-07-21 | **메카천수관음 전면 개편.** ① 본체: P52 컨셉 기반 Hunyuan 재생성(부조 벽 포함 — 등판 배치 전제) ② 팔: Meshy 부품 6종(상완/팔꿈치/전완/피스톤/손목/손) 리지드 조립 — 관절 빈 오브젝트 체인이라 굽힘 찢김 없음, 전완 슬리브 신축 추적으로 손목 분리 방지 ③ 등판 장착 20팔 후광(동일 크기 2열 교차, k1.45, 도달 ~27m), 왕관 4팔 장식 전용 ④ 타격: 육중 타이밍(예비 1.3s/홀드 0.5s/꽂힘 0.75s) + 망치 장전(-95° 접힘) + 스톱모션 경련 낙하(5단 양자화, 단마다 증기) ⑤ 관절 증기 3식×20(백색 고불투명·굵은 입자, 기동/예열/착지 버스트) ⑥ 몸통 관통 방지: 배차 게이트(보스 중심 방위 75°+도달)+최소 반경 9m 클램프, 숙임 제거 ⑦ PlayerHealth 디버그 무적 토글(_godMode — 커밋 시 OFF) | 다음 개체 컨셉아트 제작 계속 / 해태 보스전 | 루트 회전 정규화 필수 교훈: 플레이 잔재 회전이 씬에 저장되면 이후 로컬 공간 조립·실측이 전부 뒤틀린다. 보스 정면=+Z. 배차 게이트로 도달 밖 위치에선 슬램 대신 타 패턴 사용 |
| 2026-07-21 | **도깨비불 완성 — 무정형 개체 표준 확립.** 구조: 심지(Hunyuan 달걀형, 0.34m, 35% 반투명 URP Unlit) + 2겹 화염 파티클(속 300/겉 220개/초, 수직 상승 기둥) + 얼굴 탈(컨셉 원화 알파 스프라이트 쿼드, 20% 반투명) + 불씨 + 청색 점광원. WispFloat(부유·표류·맥동·요 빌보드·광원 일렁임) 신설, Model(빌보드)/Mesh(세움 보정) 2단 구조로 빌보드-보정 충돌 해결. 4기 재배치(자폭형 유지) | 다음 개체(고체형 — 객귀·와귀 등) 생성. Hunyuan 제출 후 다른 작업 병행 권장 | Hunyuan은 화염·연기류에 4연속 실패(카드 2·큐브 1·달걀 1, 100크레딧 소모 — 잔여 약 150). 교훈: 무정형은 생성 없이 스프라이트+파티클 직행(화마·한도 동일). 실루엣 없는 입력(배경 회색·프레임 꽉 참)은 카드/큐브가 됨. 서버 큐 30~50분/건인 날 있음 | 
| 2026-07-21 | **적 카탈로그 로스터 + 앙괭이 풀 파이프라인 확립.** ① 적 스펙 시트 정성판 v0.1·아키타입 카탈로그 v0.3 기준 로스터(EnemyDefinitionSO — 아키타입 9종: 원거리/속박/화탄/참격/끌기/돌진/경화순환/자폭/도둑, 내구 사다리 [가정] HP)·EnemyAI 아키타입 분기. ② 모델 생성 3계보 비교(Meshy 텍스트→10종 / Hyper3D / Hunyuan 이미지) — **Hunyuan 채택**(컨셉아트 기반, 25크레딧/건·잔여 275). ③ 앙괭이 시범: Hunyuan 생성 → Mixamo 자동 리깅+애니 5종(Idle/Walk/PickUp/Attack/Death) → Blender 흔들림 본 13개 합성(치마 4방향·불길, 웨이트 인덱스 직통 이식·임계 0.15) → Unity AC_Anggaengi(Idle↔Walk 실측 속도, PickUp→Walk 연결, 사망 지연 파괴 2.5s)·EnemyAnimatorDriver·SpringBoneSway(버렛 스프링 — 옷 처짐/불길 부력+바람). ④ 씬 정리: 모델 기반 15기 제거, 앙괭이+보스+더미 6기(빌보드 한글 라벨)만 유지 | 다음 컨셉아트 개체 순차 제작(생성→Mixamo→합성→배선). 네발짐승은 절차 모션, 무정형은 리깅 없이 | Mixamo는 CC 구독 무관 무료. Hunyuan 애드온 몽키패치는 Blender 재시작 시 소멸(영구화 미결). Attack 클립은 컨트롤러 미배선(용도 미정) |
| 2026-07-20 | **받침 거동 5종 효과 구현·검증** — FinalModifierConfigSO(수치 데이터), EnemyStatus(속박·지속·격발 표식 수신, 런타임 부착), SpellProjectile 관통(최대 3)·연쇄(감쇠 점프, 방문 집합 공유), 상합 설치 스텁 해제(ㅁ받침=표식 설치→다음 술식 적중 시 격발 x2.2 [가정]), EnemyAI 속박 감속 연결. 격발 5x5 매트릭스는 미결 — 확정 시 데이터 교체 구조. 배선 유실 버그(도메인 리로드 타이밍) 재배선+3중 검증으로 해결, 미배선 경고 로그 추가 | 받아치기·그로기 | 로그 검증: 각=속박, 검/감/남=설치, 아/사=격발 격파 확인 |
| 2026-07-20 | **W3 종료** — D21 졸개 텔레그래프 3종 이식(EnemyAI 상태머신, 페인트=느린 색 위장, 패턴별 투사체 크기·속도 차별화, EnemyDefinitionSO 데이터화) + D22 한 합 검증(검증장 졸개 2기). 사용자 검증 통과 | 씬 방 구성 → W4 | 페인트 위장 색이 의도임을 코드 주석에 명시 |
| 2026-07-20 | **보스 v2 완성·검증 — 시그니처 보스 프로토 종결.** 팔 단일 모델 통일(변형 3종 중 Open 채택·Mudra/Cup 삭제) + 메시 3분할(상완/전완/손) 절차 리깅, 어깨-팔꿈치-손목 3관절 내려찍기(치켜들기→정점 홀드 떨림→과회전 낙하→이중 충격파+CameraShake→반동→꽂힘 유지), 텔레스코픽 피스톤 신장 2.0배(황동 로드)+몸통 숙임 12도, 치켜들기 몸통 상방 통일+손바닥 지면 지향(따귀 스냅 +55→-55), 유휴 3겹(유기 흔들림+손목 굽이·비틀림+경련, 팔꿈치 래칫), 2페이즈(HP 절반: 볼리 4팔 스태거+콤보 4연타, frantic 0.6배속, **타격점 겹침 방지 산탄**), 전체 1.4배(전고 25.5m), EnemyHealth 에디터 체력 버튼. Meshy 파이프라인+전투 시스템 검증 완료(사용자) | D21 잔여(졸개 이식)·D22 | LookRotation 업힌트 누락=롤 뒤집힘 교훈. 팔 모델 손바닥 축은 +Z였음(실측 필수) |
| 2026-07-20 | 보스 v2 — **스팀펑크 메카천수관음 12m**: 몸통+기계 팔 단품 분리 생성(Meshy), 팔 10개 방사 리지드 배열, **팔 내려찍기 절차 애니**(치켜들기 텔레그래프→조준 확정 낙하→착지 광역→복귀, 스키닝 없이 기계 부품 리지드 원칙), BossAI 패턴 통합(40% [가정]), HP 800 | 플레이 검증 -> 커밋 | 리깅 질문 답: 통짜 메시 자동 리깅은 비추 — 분리 생성+리지드+절차 모션이 정석(CLAUDE.md 기계 유닛 원칙) |
| 2026-07-20 | D20(부적 1~3키 즉발·먹 무소모·수량, 붓 잔량 틴트) + **메카천수관음 프로토** — Meshy 생성(6m 정규화, Z-업·단위 보정), EN_MechaGuanyin(금·HP400 [가정]), BossAI 텔레그래프 3종 패턴(D21 선행) | 플레이 검증 → 커밋 | Meshy FBX 교훈 확장: cm 단위 + Z-업 + 메시 바운드 이상 — 임포트 시 useFileScale 끄고 실측 기준 세우기 |
| 2026-07-19 | 설계 문서 6종 정독(서브에이전트) — 전투·먹 경제·상합 원 사양 확보, CLAUDE.md 종성/중성 절 v2.0 개정(사용자 승인). 진 효과 1차 실작동: 분류x범위 디스패치(ㅏ=투사체/ㅗ=팽창 파동/ㅓ=전방 막/ㅜ=광역 돔), 반투명 프리미티브+속성색+인식 글자 텍스트(획 원본 텍스처는 M1 폴리시), 방어막의 적 투사체 차단, 상합은 D12 후 스텁 | 플레이 검증 -> 커밋 | VFX 에셋 미사용 표현(사용자 지시). 격발용 5x5 매트릭스가 추후 별도 필요(문서) |
| 2026-07-19 | 인식·판정 개선 묶음 — JamoMatcher(원시 거리, 점수 포화 제거), 거울상 중성 기하 판별(ㅏㅓ/ㅗㅜ), 분할 구조 검증(직선 중성 배제 — 나→모·누·소 오인식 해소), 약발동(A안: 최악 자모 거리>1.8 [가정] 시 위력 50%·작은 투사체), Q 홀드 방식(떼면 판정), 소멸 연출(정발동 글로우/약발동 무효과 페이드) | 커밋 완료. M1 W3 착수 대기 | 「너」·「나」 오인식 사용자 검증 통과. ㅣㅡ 특수 중성 도입 시 직선 배제 검증 재설계 필요(주석 기록) |
| 2026-07-19 | **게이트 M0 통과·마일스톤 종료.** 번짐 +20%(1.62), 속성별 공격 색(ElementPaletteSO — [가정] 오방색 조정판, VFX 런타임 틴트). 전체 일괄 커밋 | M1 착수 — W3(회피·평타·먹 경제) 권장 | 먹선 룩 사용자 승인(「정말 괜찮아졌어」) |
| 2026-07-19 | 붓자국 텍스처를 Recraft로 생성 — 3차 시도 끝에 유효 4장 선별(조류·붓 오염물 제외), PCA 수평 보정·알파 변환·커버리지 정렬 아틀라스(`_Project/Art/Ink/T_InkStrokeAtlas.png` 4행). InkStroke를 스트레치 UV(획=텍스처 1장, 기필·수필 내장)·획당 행 선택(먹 잔량+무작위)으로 전환. 굵기 0.018·순흑 반영 | 룩 체감 확인(사용자) | 프롬프트 교훈: "flying white"는 새로, "brush"는 붓 사진으로 오해됨 — 원본 후보는 Tools/generated에 보존 |
| 2026-07-19 | 먹선 붓글씨 질감 — InkStroke 절차 메시(LineRenderer 대체): 점별 굵기(속도+노이즈)·정점 농도(속도+먹 소모)·비백 아틀라스(6단 마름, 코드 생성)·기필 눌림·수필 빼기·글자 단위 먹 소모(뒤 획 갈필)·번짐. Lerp 8로 무겁게. 파라미터 전부 인스펙터 [가정] | 룩 체감 확인(사용자) | 사용자 제공 서예 레퍼런스 기준. 셰이더 저작 없음(정점색+생성 텍스처) |
| 2026-07-19 | 허수아비 원거리형 전환(9m 정지·텔레그래프 후 무유도 투사체 — 회피 가능), Meshy 파이프라인 첫 가동: 대필 FBX+텍스처 생성·임포트·BrushRig 조립(크기 정규화 0.35m·ViewModel 레이어), BrushViewModel(교전 대기<->작도 커서 추종), 먹선 투영 카메라 VM 통일 | 플레이 확인(붓 방향 포함) → 게이트 M0 판정 | 붓끝이 반대로 보이면 SM_Brush의 Y회전 -90 -> 90 |
| 2026-07-19 | 인식 방식 개선: 합성 글자 템플릿 → **획 그룹 분할 인식**으로 교체(모든 분할점 시도, 부분 독립 정규화, 초성/중성 후보군 분리) — 침범·초성 오인식 해소, 사용자 체감 대폭 개선. API 키 4종(Meshy·Recraft·ElevenLabs·SuperTone) 인증 검증 | 일괄 커밋 → D8 잔여 | 자모 사이에는 펜을 떼야 함(이어 쓰면 분할점 없음) |
| 2026-07-19 | 이어 그리기 인식(합성 글자 템플릿, 배치 2종·변형 16/클래스), 중성 실필기 등록 모드(F2, 1~4 저장·즉시 재구성), 획 굵기 0.006, 좌클릭 임시 시전 제거, 투사체 접촉 즉시 소멸+VFX 3초 상한(최적화), 먹선 카메라 로컬 공간(화면 추종), API 키 스캐폴딩(`Tools/.env.example`+gitignore — Meshy·Hyper3D·Recraft·Suno·ElevenLabs) | 인식률 재체감 → 일괄 커밋 | 종성 확장은 종성 템플릿 등록 후 동일 조합 방식 |
| 2026-07-19 | 먹선 스타일(붓끝 Lerp·속도 굵기·번짐 애니·소프트 엣지 텍스처 코드 생성, 전 파라미터 [가정] 인스펙터 노출), 락온 떨림 수정(피치 로컬 회전 + 지수 감쇠 — 월드 회전 피드백 제거), 2단계 작도(초성→중성, 합성 글자 발동). 중성 템플릿 ㅏㅓㅗㅜ 합성 12개 생성(추후 실필기 교체 가능) | 통합 플레이 검증 → 일괄 커밋 | 종성 확장은 M1 D12 |
| 2026-07-19 | 렌더 파이프라인 복구 — GraphicsSettings에 URP 미지정 상태 발견, `_Project/Settings/URP_PipelineAsset`+`URP_Renderer` 생성·Graphics/품질 6레벨 지정. 갈색 바닥·붓대(먹빛)·허수아비(회색) URP 머티리얼. 작도 시전 파이프라인(초성→ㅏ기본형 매핑), 락온(Tab·범위 20m [가정]), 작도 중 이동 허용(시점만 잠금) | 통합 플레이 검증 → 커밋 | 프리미티브 생성 시 기본 머티리얼은 Built-in이라 URP에서 마젠타 — 프로토 머티리얼로 교체할 것 |
| 2026-07-19 | D3 플레이 검증 완료(4단 로그 확인)·커밋 00819b0. D4 — StarterAssets FPC 채택, InGame 씬 배선(플레이어 프리팹+바닥, 기본 카메라 제거), 카메라 스크린샷 확인 | D4 플레이 검증 → 커밋 → D5 | 에디트 모드 스크린샷의 카메라 위치는 Cinemachine 작동 전 상태 — 정상 |
| 2026-07-19 | D2 완료 확인(부트 플레이 검증·Odin 임포트, 커밋 9e24011) + D3 구현 — SO 이벤트 채널 코어(제네릭+Void, 리스너, 에디터 디버그), 서비스 4종 인터페이스·스텁 DI 등록, AppEntryPoint 생성자 주입 전환. 컴파일 에러 0 | 플레이 재검증 후 D3 커밋 → D4 | 리포는 private — Odin 커밋 가능 확인 |
| 2026-07-19 | M0 D2 구성 — VContainer 1.19.0·UniTask 2.5.11 설치, Core asmdef 참조 추가, RootLifetimeScope·AppEntryPoint 작성, Boot/InGame 씬 + 빌드 세팅 배선. 스파이크 커밋(89a4c3a) | 부트 흐름 플레이 검증·Odin 임포트(사용자) → D3 | MCP 도구가 도메인 리로드를 사이에 두면 응답이 끊기는 이슈 — 패키지 설치·스크립트 컴파일 후에는 에디터 재시작이 가장 확실. RootLifetimeScope 컴파일 누락도 재시작으로 해소 |
| 2026-07-18 | S2~S4 완료 — 실기 마우스 테스트 정확 인식(사용자 확인), 템플릿 23개(`Assets/Spike/Templates/`), 다획 네이티브 실증. $P 채택을 결정 로그에 기록 | M0 D2 — VContainer·UniTask·Input System 순 도입 후 Odin | 스파이크 폴더(Assets/Spike, Spike/)는 M1 W1에서 본 시스템 이식 후 정리 예정. 템플릿 XML은 M1 D10 재사용 가치 있음 — 보존 |
| 2026-07-18 | Unity MCP 로컬 모드 연결 확립(클라우드 OAuth 불가 → bootstrap-local). S2 준비 완료 — `Assets/Spike/S2_JamoRecognitionTest.cs` 작성, `Assets/Spike/S2_JamoTest.unity` 씬 구성(카메라·라이트·컴포넌트 부착), 컴파일 에러 0 | 사용자가 플레이모드에서 ㄱㄴㅁㅅㅇ 템플릿 등록(클래스당 3~5개) 후 테스트 모드로 인식률 측정 | 플러그인 설정상 playmode 제어 도구 비활성 — 플레이 진입은 사용자가 직접 |
| 2026-07-18 | S1 완료 — PDollar($P) 콘솔 구동 검증(Spike/S1_PDollarConsole). 16종 템플릿 자기 분류 16/16, 교란 인식률 98.8%, 지연 평균 0.59ms | S2 — 초성 5자 템플릿 + 마우스 입력 인식률 | Unity MCP(ai-game-developer) 미인증 상태 — `npx unity-mcp-cli login` 필요. $P는 회전 비불변이므로 자모처럼 방향이 의미 있는 글자에 유리 |
| 2026-07-18 | M0 D1 완료 — 초기 세팅 확인(Force Text·Visible Meta·Linear·URP), .gitattributes(LFS) 작성, Assets/_Project 폴더 구조 + asmdef 6종 생성, 루트 CLAUDE.md 연결, 초기 커밋 | S1 스파이크 검증 또는 D2(VContainer·UniTask·Input System → Odin) | PDollar($P 인식기)가 이미 Assets에 임포트되어 있음 — S 스파이크를 본 프로젝트 내에서 바로 진행 가능. Unity 에디터에서 다음 열기 시 asmdef 컴파일 확인 필요(빈 어셈블리 경고는 무해) |
| 2026-07-18 | CLAUDE.md 갱신, PROGRESS.md 신설 | S1 착수 여부 결정 → M0 D1 | — |
